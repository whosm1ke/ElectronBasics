# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

**Snippet Runner** — a local, Windows-only shell snippet launcher in the style of Raycast/Spotlight: a frameless, always-on-top, global-hotkey-activated Electron window for storing, searching, and running one-liners or multi-step sequences across **seven shells** (PowerShell, CMD, Git Bash, WSL, Node.js, Python, SSH), with run history, tags, pinning, parameterized commands, reusable/environment/computed variables, output-capture into variables, saved snippet groups, branching visual pipelines (with delay/gate/sub-pipeline/group nodes, AND/OR joins, retries, concurrency caps, and their own schedule), background/long-running processes, scheduling, external HTTP/file-watch triggers, subscribed external snippet libraries, a Health panel, a Ctrl+K command palette, batch runs with live output, run-after chaining, assertions, themeable UI, and self-update from GitHub Releases.

**There is no destructive-command guard anywhere in this app.** An earlier version had one (pattern-matched confirm-click in the UI, re-enforced in the main process, hard-skipped by the scheduler); it was deliberately removed in full — no trace of it should reappear. A command runs exactly when the user triggers it — directly, scheduled, chained, or batched — full stop. If you're asked to add any kind of "is this command dangerous" check back, don't; point back to this file instead.

**TypeScript + React, built with electron-vite — no `.js` files in the source tree.** The app was originally plain CommonJS main/preload with a framework-free native-ES-module renderer and no build step; it has since been migrated to TypeScript throughout main/preload, and to TypeScript + React (with Zustand) for the renderer, via a strangler-fig migration (old modules ported one at a time behind temporary re-export shims) that has since been fully cut over — every shim was deleted and every consumer rewired to import the real module directly (see **Renderer architecture** below for what the renderer's file layout looks like as a result). The one deliberate exception is `build/*.js` (`afterPack.js`, `beforePack.js`, `generate-icon.js` — see its own note in **Renderer architecture**'s closing paragraph) — electron-builder `require()`s its `beforePack`/`afterPack` hooks directly at build time, outside the Vite/tsc pipeline, so they have to stay plain Node JS.

## Commands

- **Dev**: `npm start` runs `electron-vite dev` — a local Vite dev server for the renderer with HMR, main/preload rebuilt and the app relaunched on change.
- **Typecheck**: `npm run typecheck` (`tsc --build --noEmit`, project-referenced across `tsconfig.node.json` main/preload and `tsconfig.web.json` renderer+shared).
- **Unit tests**: `npm test` (`vitest run`) — currently covers main-process pure logic: `sanitizeSnippet`/sibling sanitizers, `buildInvocation`'s per-shell argv construction, and the cron/schedule matcher. Add tests alongside new pure logic as you touch it; there's no coverage gate, but untested pure logic in `src/main/shell/exec.ts` or `src/main/scheduler.ts` is the highest-blast-radius code in the app to leave unverified.
- `electron-builder` drives packaging: `build`/`build:portable` scripts (`electron-vite build && electron-builder --win` / `--win portable`).
- **Cutting a release** (for the in-app updater — see `src/main/updater.ts` below): `npm run release`. The version bump is automatic — `release`/`release:minor`/`release:major` each run `npm version <patch|minor|major> --no-git-tag-version` (bumps `package.json`+`package-lock.json`, no git commit/tag) before building and publishing, specifically so a forgotten manual version bump can't ship a release electron-updater won't recognize as newer. Default to plain `npm run release` (patch) unless you deliberately want a bigger jump. This needs a `GH_TOKEN`/`GITHUB_TOKEN` env var (a GitHub PAT with at least `public_repo`) set on *this* machine — it uploads the NSIS installer, the portable `.exe`, and `latest.yml` to a GitHub Release on this repo (`build.publish` in `package.json`). Installed copies elsewhere need no token to read a public repo's releases; they only ever check/download, never publish. Commit and push the resulting `package.json`/`package-lock.json` version bump afterward so the repo matches what shipped.
  - **`node build/create-github-release.js` runs between the version bump and the build, creating the GitHub release for the new tag up front — before electron-builder ever touches GitHub.** Without this, electron-builder's own "does a release exist yet for this tag?" check races itself: it fires once per artifact it's about to upload (the installer, its `.blockmap`, the portable `.exe` — even just the installer + its blockmap from ONE target is enough), each check runs concurrently, and more than one of them independently decides "no" and tries to create the release. This was confirmed twice in practice, two different ways: once as a hard 422 ("Published releases must have a valid tag") that killed the whole process outright, and once — no error at all — as two silently-created release objects for the identical tag, each ending up with only some of the artifacts (`latest.yml` and the installer in one, the portable `.exe` and the blockmap in the other). Splitting the two Windows targets (`nsis`, `portable`) into separate sequential `electron-builder` invocations (see the `release` scripts) does NOT fix this on its own — the race reproduced even inside a single nsis-only invocation, since it's an artifact-level race within electron-builder's own publish pipeline, not a target-level one. Pre-creating the release removes the ambiguity every one of those concurrent checks would otherwise race over: by the time electron-builder runs, "does a release exist" is unambiguously yes, so every artifact just uploads to the one that's already there. `create-github-release.js` is idempotent (a no-op if the release already exists, e.g. re-running a release attempt that failed after this step) and reads the same `GH_TOKEN`/`GITHUB_TOKEN` electron-builder itself uses.
- **`build.publish.releaseType` must stay `"release"`.** electron-builder's own default is `"draft"` — a `--publish always` run then creates the GitHub release as a *draft*, visible in the repo's own Releases tab (to the owner) but invisible to the public releases feed `electron-updater` actually reads. The exact symptom is the in-app "Check for updates" button failing with **"No published versions on GitHub"** even though the release is right there on GitHub — because it's a draft. If that ever happens again: check whether the newest release on GitHub is marked "Draft" and click Publish on it by hand (a draft doesn't retroactively become non-draft just by fixing this config for the *next* release).
- **`electron-updater` must stay a `dependencies` entry, never `devDependencies`.** `electron-builder` only packs production dependencies into the built app by default — if it's under `devDependencies`, `npm start` still works fine (local `node_modules` has everything either way) but the *installed* app silently ships without it and `require('electron-updater')` throws the moment Settings tries to check for updates. This exact mistake has happened once already; it's an easy one for a package-manager UI or a careless `npm install` to reintroduce. Re-verify this after any `package.json` edit. `react`/`react-dom`/`zustand` must stay `dependencies` too — they ship inside the renderer bundle at build time either way, but `dependencies` is the correct home for anything the shipped app's own code imports.
- **CI** (`.github/workflows/ci.yml`): `typecheck` + `test` on every push/PR. No release workflow — releases stay a manual, local `npm run release` per the `GH_TOKEN` requirement above.
- To verify a renderer or main-process change beyond typecheck/tests, actually launch the app (`npm start`) and exercise it — there's no snapshot/component test suite.

## Toolchain: electron-vite

One `electron.vite.config.ts` builds all three processes (main/preload/renderer), each its own Vite/Rollup sub-config. In dev it runs a local Vite dev server for the renderer (HMR) and rebuilds main/preload on change; in production it emits static output to `out/main`, `out/preload`, `out/renderer` (gitignored — separate from electron-builder's own `dist/` output dir, no collision) and the app loads it via `loadFile`, same as before the migration. `package.json`'s `"main"` points at `out/main/index.js`; `build.files` is `["out/**/*", "package.json"]`.

**Minification**: `electron-vite` hardcodes `minify: false` as its own internal default for all three sub-configs, overriding Vite's own normal production default (`'esbuild'`) — a project that never explicitly opts back in ships completely unminified JS from a real `electron-vite build`, not just in `npm start`'s dev server. The `renderer` sub-config here sets `build: { minify: 'esbuild' }` specifically to undo that (main/preload are deliberately left at electron-vite's own default — unminified `console.error` stack traces from a shipped app's own logs are worth more there than the download-size win, since neither process's code is ever parsed/painted by something a user perceives as "app load time" the way the renderer bundle is). If a build ever looks suspiciously large or you're checking whether this is still in effect, `out/renderer/assets/*.js` should read as one dense, minified line, not formatted multi-line source.

**CSP**: production `index.html` keeps its strict CSP (`default-src 'self'; script-src 'self'; ...`) byte-for-byte. A dev-only `transformIndexHtml` plugin (`devCspPlugin()` in `electron.vite.config.ts`) swaps in a relaxed CSP only when Vite's dev server is active — `@vitejs/plugin-react`'s Fast Refresh preamble and the Vite client's `ws://` HMR socket need it, but it must never ship in the packed app. If you touch this plugin, verify with a production build (`npm run build:portable`) that the shipped `index.html`'s CSP meta tag is unchanged.

`tsconfig.json` is a solution file referencing `tsconfig.node.json` (main+preload, Node types) and `tsconfig.web.json` (renderer+shared, DOM+React types) — `tsc --build` compiles both project references. **TypeScript 7's native compiler removed `baseUrl` support entirely** — path resolution here goes through Vite's `resolve.alias` (`@shared` → `src/shared`) at build time and each tsconfig's own `paths` at typecheck time, not `baseUrl`. If `tsc --build` starts failing after a tsconfig edit with a stale-looking error, delete the `.tsbuildinfo` incremental-build caches first (gitignored) before assuming the fix is wrong.

## Architecture

### Three-process split (standard Electron security model), each its own folder

- **`src/main/`** — the only side with Node/OS access, all TypeScript. `index.ts` is a thin entry point that does nothing but wire the pieces together (create the window/tray/icon, register the hotkey with fallback, register IPC handlers, start the scheduler, handle `app` lifecycle events) — no logic of its own lives there. Everything else in `src/main/` is one concern per file: `window.ts` (the BrowserWindow), `tray.ts`, `hotkey.ts`, `icon.ts` (the hand-rolled PNG encoder), `ipc.ts` (every `ipcMain` handler, delegating out), `scheduler.ts` (the background tick), `shell/exec.ts` (the multi-shell execution engine), `shell/terminal.ts` (opening a real terminal window), `storage/*.ts` (one file per persisted JSON concern), plus tiny shared leaves (`paths.ts`, `ps-quote.ts`, `env-utils.ts`) — `newId()` used to be one of these (`id.ts`) but now lives in `@shared/id` (see below), since the zod schemas in `@shared/types` need it too. `electron-augment.d.ts` extends Electron's own `App` interface (`app.isQuitting?: boolean`) — note it must use `declare namespace Electron { interface App {...} }`, not `declare module 'electron' { interface App {...} }`, since Electron's real `App` type lives inside that namespace internally and only a matching namespace declaration merges with it.
- **`src/preload/index.ts`** — the *only* bridge between main and renderer. Runs with Node access but exposes nothing except a curated `window.electronAPI` object via `contextBridge.exposeInMainWorld`, written `satisfies ElectronAPI` against the shared contract (`src/shared/types/electron-api.ts`) so drift between preload's actual surface and what the renderer thinks it can call is a type error, not a silent runtime mismatch. `nodeIntegration: false`, `contextIsolation: true`, `sandbox: true` in `BrowserWindow` — the renderer never gets a raw `ipcRenderer` or `require`.
- **`src/renderer/`** — TypeScript + React, **no Node access**, everything it needs from the OS goes through `window.electronAPI.*`, which round-trips to an `ipcMain.handle`/`ipcMain.on` in `src/main/ipc.ts`. See **Renderer architecture** below for the current layout — it's mid-migration-shaped, not a clean-sheet React app, and that shape is deliberate in places.
- **`src/shared/types/`** — types imported by both main and renderer via the `@shared/types` alias (barrel `index.ts`). **The persisted-data shapes are zod schemas, not hand-written interfaces**: `Snippet`/`EnvVar`/`ExpectConfig`/`ScheduleConfig` (`snippet.ts`), `Variable` (`variable.ts`), `Group` (`group.ts`), `Pipeline`/`PipelineNode`/`PipelineEdge` (`pipeline.ts`) are all `z.infer`'d from a schema in that same file — the schema *is* the single source of truth (both the compile-time type and the runtime backfill/coercion logic `storage/*.ts`'s `sanitize*()` functions used to hand-roll separately), not a compile-time mirror of logic living elsewhere. Each is `z.unknown().transform(normalize).pipe(OutputSchema)` — see `snippet.ts`'s header comment for why the transform preserves the old sanitizers' exact coerce-and-default bodies rather than being rewritten with zod's native validators (which reject invalid input; this codebase's contract is to backfill it, never reject it), and why `.pipe(OutputSchema)` (not just an annotated return type) is what makes the type genuinely schema-derived. `AppSettings`, `HistoryEntry`, `RunResult`/`SequenceResult`/`DebugInfo` (`run.ts`), `ProcessStatusValue`/`ProcessSnapshot`/`ProcessOutputEvent`/`ProcessStatusEvent` (`process.ts`), small IPC result shapes (`misc.ts`: `ImportSnippetsResult` as a real discriminated union on `ok`, `UpdateStatusEvent`, …), and the full `ElectronAPI` interface (`electron-api.ts`) preload is typed against stay plain hand-written interfaces — no sanitizer ever backfills these from untrusted disk JSON the same way, so there's no matching "two definitions" problem to close. A couple of unions carry an `'idle'` member that main-process code never actually sends — it's the renderer's own default/sentinel value before any real event has arrived (e.g. `UpdateStatusValue`, `ProcessStatusValue`) — documented inline at each occurrence so it isn't mistaken for a missed case. `src/shared/id.ts`'s `newId()` is the one plain (non-type) shared runtime value here — needed by the zod schemas' id-backfill logic, and re-exported (not reimplemented) by both the renderer's `lib/utils.ts` and, formerly, `src/main/id.ts` (deleted — main-process code now imports it from `@shared/id` directly).

### Multi-shell command execution (`src/main/shell/exec.ts`)

`runShellCommand(command, {cwd, shell, elevated, env, stdin, debug})` is the single execution engine, built on `child_process.execFile` with explicit argv arrays per shell — **not** `exec()`'s shell-string convenience, which on Windows always wraps the command as `<shell> /d /s /c "<command>"` regardless of which binary you name; that convention is only correct for `cmd.exe` (and happens to work for `powershell.exe`), but would silently misinterpret it for `bash.exe`/`wsl.exe`/`node`/`python`. `buildInvocation(command, shellType)` returns `{candidates, args}` per shell:

- **powershell** (default): `powershell.exe -NoProfile -NonInteractive -Command <preamble+command>` — the UTF-8 preamble (`[Console]::OutputEncoding = ...; chcp 65001`) is load-bearing, don't strip it.
- **cmd**: `cmd.exe /d /s /c "chcp 65001 > nul && <command>"`.
- **gitbash**: `bash.exe -lc <command>` — `candidates` also tries the two standard Git-for-Windows absolute paths, since `bash.exe` often isn't on PATH even when `git.exe` is.
- **wsl**: `wsl.exe -e bash -lc <command>`.
- **node** / **python**: `node -e <command>` / `python -c <command>` — Python additionally gets `PYTHONIOENCODING=utf-8` and `PYTHONUTF8=1` forced into its env so output decodes correctly on Windows.
- **ssh**: `ssh.exe -o BatchMode=yes -o StrictHostKeyChecking=accept-new -p <port> [-i <identityFile>] <username>@<host> <command>` — runs on a remote host over Windows' built-in OpenSSH client (`SHELL_CANDIDATES.ssh` tries plain `ssh.exe` on PATH, then its fixed `System32\OpenSSH` location). `BatchMode=yes` is load-bearing: without it, a host that needs a password or a first-time host-key prompt would hang the run forever waiting on stdin that never comes. `buildInvocation()` takes an extra `ssh: SshConfig | null` (host/port/username/identityFile, `Snippet.ssh`) and `cwd` parameter for this case only — a *local* `cwd` execFile option has no meaning for a process that isn't spawned on this machine, so `runShellCommand` instead folds `cwd` into the remote command text (`cd '<cwd>' && <command>`) and passes `cwd: undefined` to `execFile`.

There is no "is this command dangerous" check anywhere in this file, or anywhere in the app — running a command is entirely the caller's responsibility (see the top of the file for the comment making that explicit).

`candidates` is a fallback list tried in order via ENOENT retry (see the `tryNext()` closure) — if a shell binary isn't found, the *next* candidate is tried before giving up with a clear "could not find a … executable" error, rather than a bare ENOENT.

`elevated: true` only works for `shell: 'powershell'` — `runShellCommand` returns an explicit error for every other shell rather than silently running the command as the wrong interpreter. It wraps the command in a non-elevated PowerShell script that calls `Start-Process -Verb RunAs -Wait -RedirectStandardOutput/-RedirectStandardError` to temp files, then reads them back and stitches stdout/stderr apart using the `ELEVATED_MARKER` sentinel. Keep the marker-split logic and the `-EncodedCommand` (base64 UTF-16LE) encoding if you touch this path — it's what avoids quoting hell for arbitrary inner commands.

`stdin` is written to `child.stdin` immediately after spawning, then `child.stdin.end()` is **always** called (even with no stdin) so a command that waits on input never hangs a run. `debug: true` attaches `{file, args}` (or a fixed note for the elevated path) as `result.debugInfo`, surfaced by the renderer only when Developer mode is on.

Multi-step snippets run through a *separate* `run-sequence` IPC channel/handler (`src/main/ipc.ts`) that loops `runShellCommand` per step and returns per-step results plus an aggregate history entry — it is not just `run-command` called in a loop from the renderer. Sequences support `env` but not `stdin` (scope limitation, not a bug).

### Opening a real terminal (`src/main/shell/terminal.ts`)

`openTerminal({command, cwd, shell})` opens a real, visible, interactive console window pre-loaded with the command. **Do not implement this as a direct `spawn(shellExe, args, {detached: true, stdio: 'ignore', windowsHide: false})`** — that was the original approach and it does not reliably produce a visible, persisting window when the spawning parent is an Electron main process on Windows (a console-subsystem child with no console of its own doesn't reliably get a fresh one allocated the way it does when typed into an already-running shell). The working, current implementation instead goes through `cmd.exe /c start "" <shell> ...`, which explicitly asks the shell to allocate a brand-new console window for the child — the same mechanism VS Code's "Open in terminal" and Explorer's "Run" ultimately rely on. The command itself is passed to PowerShell via `-EncodedCommand` (base64 UTF-16LE) rather than `-Command "<text>"`, so it never has to survive being re-quoted across three layers of argv parsing (cmd → start → powershell). `windowsVerbatimArguments: true` is required alongside this — Node's default Windows argument-escaping would otherwise mangle the `start ""` empty-title token.

If you need to verify this works after touching it: `Get-Process powershell` alone is **not** a reliable check — a console app's actual on-screen window is owned by `conhost.exe`/`OpenConsole.exe`, not by the console app's own process, so checking `(Get-Process powershell).MainWindowHandle` will read `0` even for a perfectly visible window. Check for a *new* `conhost`/`OpenConsole` process (by `StartTime`) instead, or just take a screenshot.

### The dangerous-command guard was removed — don't re-add it

An earlier version of this app had `DANGEROUS_PATTERNS` duplicated in both the main and renderer processes, an "arm/confirm" Run button, a `confirmed`/`requiresConfirmation` IPC contract, and hard-skips in the scheduler/batch-run/run-all/run-after paths. **All of it is gone.** `runShellCommand`, `run-command`/`run-sequence` in `ipc.ts`, `tickScheduler` in `scheduler.ts`, and every renderer run path execute unconditionally — there is nothing in this codebase that inspects a command's text to decide whether it's safe to run. This was an explicit, considered product decision (the user owns the consequences of what they run), not an oversight — see the note at the top of this file. The only thing still filtered out of *unattended* paths (scheduler, run-after chaining, "Run all" on a group, batch run, pipeline run) is a snippet with unresolved `{{placeholder}}` tokens — that's a functional necessity (there's nowhere to prompt for a value in a background/batch context), not a safety gate, and should stay.

### Snippet data model & storage (`src/main/storage/`)

Snippets and run history are plain JSON files in `app.getPath('userData')` (paths centralized in `paths.ts`: `SNIPPETS_FILE`, `HISTORY_FILE`, `APP_SETTINGS_FILE`, `VARIABLES_FILE`, `GROUPS_FILE`, `PIPELINES_FILE`, `LIBRARIES_FILE`), not a database. `SnippetSchema` (`@shared/types/snippet.ts`, a zod schema — see that file's own header comment and the **`src/shared/types/`** bullet above) is the schema's single source of truth, both compile-time (the `Snippet` type is `z.infer`'d from it) and runtime (its own backfill/coercion transform, applied on both read and write via `storage/snippets.ts`'s `sanitizeSnippet()` — now a one-line `SnippetSchema.parse()` wrapper, kept under that name since other files still call it that way). Current fields: `id, name, tag, command, pinned, runCount, lastRunAt, cwd, shell, elevated, steps, stdin, icon, notes, env, expect, runAfterThis, runBefore, stopOnStepError, schedule, background, autoRestart, externalSource, captures, ssh`. `background`/`autoRestart` are the Start/Stop-instead-of-Run toggle and its crash-restart option — see the background-process section below; the sanitizer forces `background` back to `false` whenever `steps` is non-empty, since it's single-command-only. `externalSource` is non-null only for a snippet pulled in via a subscribed library (`storage/libraries.ts`, below) — holds that library's URL. `ssh` (`SshConfig|null`) is only meaningful when `shell === 'ssh'` — see the exec.ts section above. `captures` (`Capture[]|null`, `{variable, pattern}`, capped at 10) extracts a value out of a run's combined stdout+stderr straight into a global variable — `@shared/captures.ts`'s `extractCaptures()` (group 1 of the regex if present, else the whole match) is applied by both `runEngine.ts` (a manual run) and `unattendedRun.ts` (scheduled/triggered/pipeline-step runs), each writing the result back into `variables.json` themselves (never touching a variable's own `secret` flag either way). `command` is always kept populated (joined from `steps` for multi-step snippets) so free-text search has something to match against even for sequences. `env` is `EnvVar[]|null` (sanitized by `EnvVarListSchema`, capped at 20 entries) — converted to a plain object via `envListToObject()` (shared by `ipc.ts`, `scheduler.ts`, and `unattendedRun.ts`, in `env-utils.ts`) right before being handed to `execFile`'s `env` option, merged on top of `process.env`. `expect` is `{exitCode:number|null, outputContains:string|null}|null` (sanitized by `ExpectSchema`) — checked entirely client-side in `runEngine.ts`'s `checkExpectation()`, main just stores it. `schedule` is `{enabled,type,intervalMinutes,dailyTime,cronExpr,lastRunAt,paramValues}|null` (sanitized by `ScheduleSchema`) — see the scheduler section below; `paramValues` is `@shared/types/paramValues.ts`'s shape, covered in the unattended-runs section further down.

`DEFAULT_SNIPPETS` in `storage/snippets.ts` seeds a fresh install and must be kept passing through the same field set the sanitizer expects (see the trailing `.map()` on that array).

The other storage modules follow the exact same ensure/read/write pattern — copy that pattern rather than inventing a new one if you add another persisted concern:
- **`storage/app-settings.ts`** (`readAppSettings`/`writeAppSettings`) — `{ hotkey, hasShownTrayHint, windowSize, trigger }`. `trigger` (`{enabled, port, token}`) configures the external HTTP trigger server (`triggerServer.ts`, below) — a missing/blank token is generated on first read via `crypto.randomBytes`, so it backfills the same way a missing field does everywhere else in this app.
- **`storage/variables.ts`** (`readVariables`/`writeVariables`, sanitized by `sanitizeVariable`) — reusable `{id, name, value, secret, computed}` placeholder values. The renderer's `ParamForm.tsx`/`variables.ts` prefill a parameterized snippet's inline form from these by matching placeholder name to variable name, and quietly write back an updated value after every run (`syncVariablesFromValues`) — it never auto-creates a variable that didn't already exist, only keeps existing ones fresh. A `secret: true` variable's `value` is encrypted at rest via Electron's `safeStorage` (DPAPI on Windows, tied to the current Windows user account) — encrypted *after* `sanitizeVariable()` on write (so the 2000-char cap applies to the plaintext, not the inflated ciphertext) and decrypted *before* being handed back on read, stored on disk as an `'enc:'`-prefixed base64 string. `variable.ts`'s schema gives an `'enc:'`-prefixed value a looser length cap (4000 vs 2000) for exactly this reason — don't tighten that without accounting for AES-GCM+base64 overhead over the plaintext cap. `safeStorage.isEncryptionAvailable()` is checked before every encrypt/decrypt; unavailable-at-write falls back to plaintext, unavailable-at-read (or a blob from another machine/account) fails closed to an empty string rather than throwing. `computed` (`{snippetId, refreshMode: 'manual'|'interval', intervalMinutes, lastRefreshedAt}|null`, non-null makes this a "derived" variable) is refreshed by `main/computedVariables.ts`'s `refreshComputedVariable()` — runs the referenced snippet (`executeSnippetOnce()`, no history entry, no notification — a refresh is meant to feel like reading a live value, not "running a command") and takes its trimmed stdout as the new value, either on manual demand (`refresh-computed-variable` IPC, `VariablesModal.tsx`'s Link icon) or on its own 30s-checked interval ticker (`startComputedVariablesTicker()`, called once from `index.ts`, independent of `scheduler.ts` — see that module's own header comment for why it's a separate small file rather than bolted onto the scheduler).

  Three real bugs here, all fixed and worth knowing before touching this again:
  - **A background interval-tick refresh never reached the renderer.** `state.variables` is a one-time snapshot fetched only when `openVariables()` runs (`useVariablesStore.ts`) — a tick firing in the background wrote the new value to disk with nothing to push it to an already-open Variables panel. `tickComputedVariables()` now sends a `'variables-refreshed'` IPC event (main → preload → `useVariablesStore.ts`, subscribed once at module load, same shape `onUpdateStatus`/`updater.ts` already uses) whenever it actually changes something, so a currently-open panel updates live with zero clicks.
  - **"Refresh now" looked like a no-op.** The value *was* refreshed on disk — `VariablesModal.tsx`'s value `<input>` is deliberately uncontrolled (`defaultValue`, not `value`, to avoid a full re-render per keystroke while typing), so an externally-changed prop never reached the already-mounted DOM node; React only reads `defaultValue` once, on first mount. Fixed with a small `useEffect` that imperatively re-syncs `valueRef.current.value` whenever `variable.value` actually changes — harmless during normal typing too, since by the time a blur-triggered re-render happens the DOM node's own `.value` already matches what's being set.
  - **Running a snippet substituted a stale cached value, not a live one** — the entire point of using a computed variable in something you're about to run. `ParamForm.tsx`'s own prefill just reads `state.variables` as-is; it has no idea a matching variable is computed. `lib/variables.ts`'s `refreshComputedVariablesFor(names)` (called by `Card.tsx`'s `handleRunClick`/`handleStartStopClick`, `BatchModal.tsx`'s `start()`, and `PipelinesModal.tsx`'s `usePipelineParamGate()`, always right before gating a run behind `ParamForm`) refreshes every computed variable among a run's own placeholder names — one at a time, not `Promise.all`'d, since `refreshComputedVariable` round-trips the *whole* saved list per call and two racing writes would let the second clobber the first's freshly-computed value.
- **`storage/groups.ts`** (`readGroups`/`writeGroups`, sanitized by `sanitizeGroup`) — named, saved sets of snippets (`{id, name, description, snippetIds, runCount, lastRunAt, schedule}`) you configure once and run together on demand without reselecting them. Browsing the saved list is `GroupsModal.tsx`, opened from the header — a full-window "screen" (see `.screen` in `style.css`); adding/editing one is a separate `GroupEditorModal.tsx`, a `.modal` (`openGroupEditor()`/`closeGroupEditor()`, `useGroupsStore.ts`) — a focused add/edit form doesn't need a full screen, and a `.modal` layers cleanly above whatever else happens to be open (the groups list, Details, Schedule, a pipeline's own Inspector, …) without ever needing to close it first or track where to return once it's dismissed, unlike two `.screen`s (see `lib/screens.ts`'s own header comment on why that's a real conflict for screens specifically). The one case that still needs care is Details, itself a `.modal` — DetailsModal.tsx's own group link hides Details (`hideDetailsForNavigation`) rather than leaving it open, and `closeGroupEditor()` fires the same `onEditorClosed` bus event (`lib/events.ts`) the snippet editor modal already fires on close, which is what `useDetailsStore.ts`'s existing subscription uses to reopen Details afterward — no group-specific plumbing needed. Deliberately just a list of ids, not a copy of the snippets themselves, so a group always reflects each member's current command/tag/etc.; an id with nothing behind it (the snippet was deleted) is silently skipped wherever a group is resolved into a runnable list. Running a group hands its resolved snippet list to `useBatchStore.ts`'s existing order/mode config step — same live per-row output a manually-selected batch or a tag "Run all" gets. `Card.tsx` shows a small badge on a member snippet's card (and `DetailsModal.tsx` a full "Groups" section with links straight into each group's editor) so it's obvious from the snippet itself that it belongs to one. `group.schedule` (same `ScheduleConfig` shape a snippet's own schedule uses) runs every member on a schedule — see `main/groupRunner.ts` + `scheduler.ts`'s tick. `GroupDetailsModal.tsx`/`useGroupDetailsStore.ts` are a separate, read-only companion `.modal` (a group card's Info button) — the group's own copyable ID (needed for an HTTP trigger's `/run-group/:groupId` URL — see `triggerServer.ts` below; this was the actual motivation for building it), its schedule, and every member snippet's own copyable ID, all in one place instead of opening the editor and cross-referencing each member by hand.
- **`storage/pipelines.ts`** (`readPipelines`/`writePipelines`, sanitized by `sanitizePipeline`) — branching snippet graphs (`{id, name, description, nodes, edges}`). See the dedicated section below.
- **`storage/libraries.ts`** (`readLibraries`/`writeLibraries`/`syncLibrary`/`removeLibraryAndSnippets`, sanitized by `sanitizeLibrary`) — subscribed external snippet feeds (`{id, url, name, lastSyncedAt, lastSyncCount}`). `syncLibrary()` does the actual HTTP fetch (plain `node:http`/`node:https`, capped at 2MB/15s/200 snippets) — deliberately in the main process, since production `index.html`'s CSP has no `connect-src` override and blocks a renderer-side `fetch()` to any external host outright. Each remote entry gets a stable id (`ext-<sha1(url)>-<remoteId-or-index>`) so re-syncing the same library updates the same local rows instead of duplicating them; a re-sync carries a matching row's `pinned`/`runCount`/`lastRunAt` forward rather than letting the remote JSON reset them. Removing a subscription (`removeLibraryAndSnippets`) deletes every snippet whose `externalSource` still points at that library's URL.
- **`storage/watchTriggers.ts`** (`readWatchTriggers`/`writeWatchTriggers`, sanitized by `sanitizeWatchTrigger`) — "run this snippet whenever this path changes" rules (`{id, path, snippetId, debounceMs, enabled, paramValues}`). See `main/fileWatcher.ts` below for what actually watches them, and the unattended-runs section further down for what `paramValues` resolves against.

### Background/long-running-process snippets (`src/main/shell/process-manager.ts`)

A snippet with `background: true` (editor toggle, single-command only — enforced both client-side, hiding the toggle while multi-step is on, and server-side in `sanitizeSnippet`) doesn't run-to-completion like everything else in this app; it gets **Start/Stop/Restart** controls instead of Run, for dev servers, `docker compose up`, `tail -f`, watchers, etc. This is a genuinely different execution model from `shell/exec.ts`'s `runShellCommand` (`execFile`, waits, hands back the full result) — `process-manager.ts` uses `spawn()` and keeps the child alive, streaming stdout/stderr to the renderer chunk-by-chunk as it happens over one `process-output` channel, with lifecycle changes (`starting`/`running`/`stopped`/`exited`/`crashed`/`restarting`/`restart-limit`/`error`) over `process-status`. It reuses `exec.ts`'s `buildInvocation()` for the same per-shell argv rules rather than re-deriving them.

The registry is a `Map` keyed by **snippetId**, not a separately-minted process id — this app's model is "a background snippet has at most one live instance at a time." **Stop** kills the whole process tree via `taskkill /PID <pid> /T /F`, not a plain `child.kill()` — on Windows the actual work (node/docker/ngrok…) is very often running as a *grandchild* of the spawned shell, which a plain kill would orphan running invisibly. `autoRestart: true` gives a crashed (non-zero exit, not a manual Stop) process up to 5 automatic restarts with a short delay, resetting that budget once it's stayed up a minute — so one rare crash doesn't permanently exhaust the budget for later.

Nothing here is persisted to disk or recovered after a full app restart — a background process's liveness lives only in this module's memory for as long as the main process is alive. Quitting the app (not just hiding the window — hiding is what background processes are *for* surviving) stops every one of them: `index.ts`'s `before-quit` handler calls `stopAll()` and `event.preventDefault()`s the first time through so cleanup actually finishes before the process really exits, guarded by a `quitConfirmed` flag against re-entering itself once `app.quit()` is called again.

On the renderer side, `processEngine.ts` (the TypeScript port of the original `process-engine.js` — see **Renderer architecture**) owns *all* of "how process state maps onto a card's DOM" (`syncCardBackgroundUI`) — `state.runningProcesses[snippetId]` (status + a capped, tail-kept output buffer, still on the plain `modules/state.ts` object — see below) is the source of truth, never any card's DOM, specifically because `SnippetList.tsx`'s full-list re-render can happen (search, sort, an edit elsewhere, groups changing…) faster than a long-lived background process's output. Output chunks are appended straight to the *currently rendered* card via a live `querySelector` lookup (same pattern as `runEngine.ts`'s `findCardEl`) rather than a captured element reference, so a chunk arriving while the card isn't even in the DOM isn't lost — it's still in the buffer for the next real render. This is a deliberate, permanent escape hatch (not a not-yet-componentized leftover): naive per-chunk `setState` would cause pathological re-render rates on something like `tail -f` or a dev server's stdout. `bootstrapRunningProcesses()` (called once at renderer boot) reconciles this against `listProcesses()` — needed because the main process (and any children it spawned) survives a renderer-only crash/reload even though the renderer's own JS state doesn't.

### Visual pipelines (`src/shared/types/pipeline.ts`, `src/shared/pipelineWalk.ts`, `src/main/storage/pipelines.ts` + `main/pipelineRunner.ts`, `src/renderer/src/components/Modals/PipelinesModal.tsx` + `pipeline/` + `store/usePipelinesStore.ts` + `src/lib/pipelineEngine.ts`)

A saved, named graph of existing snippets (`{id, name, description, nodes, edges, schedule, maxConcurrency}`) you build on a canvas instead of chaining one-at-a-time via a snippet's own `runBefore`/`runAfterThis` fields. A **node** (`{id, kind, snippetId, subPipelineId, groupId, delaySeconds, label, retries, retryDelaySeconds, joinMode, x, y}`) has one of five `kind`s:
- **`step`** (the original, default kind): a positioned pointer at a snippet (`snippetId`), never a copy — same "dangling id is just skipped" rule as groups. `retries`/`retryDelaySeconds` (0 by default) retry a failed attempt in place before the node counts as failed for its outgoing edges.
- **`delay`**: a pure wait (`delaySeconds`), no snippet involved — a plain orchestration primitive.
- **`gate`**: pauses an *interactive* run for a manual Continue/Abort (see the Execution section below); auto-treated as "not satisfied" on a scheduled run, since there's nowhere to ask — same "no prompting outside an attended context" rule as an unresolved placeholder.
- **`pipeline`**: runs another saved pipeline inline (`subPipelineId`), treated as one node whose result reflects that sub-run as a whole (success iff every node that ran inside it succeeded).
- **`group`**: runs every member of a saved Group inline (`groupId`), each member getting its own visible row/step rather than being collapsed the way a sub-pipeline is — a Group is a flat list, not a graph, so there's nothing more granular for a member to recurse into. Members run sequentially (same as every other "run a group unattended" path in this app — `triggerServer.ts`'s `/run-group`, `main/groupRunner.ts`), and the whole node reports one aggregate outcome for its own outgoing edges: success iff every member that actually ran (i.e. wasn't itself skipped for a missing `{{placeholder}}`) exited 0; skipped outright if every member was. Unlike `pipeline`, a `group` node can never create a reference cycle — a Group only ever points at snippets, never at another Group or a Pipeline — so it needed none of `pipelineReferenceCreatesCycle()`'s cycle guards, and every saved group is always a valid pick in the Inspector's "Change group…" picker.

`label` is a short caption used by `delay`/`gate`/`pipeline`/`group` nodes (irrelevant for `step`, which displays its snippet's own name). `joinMode` (`'any'` default | `'all'`) controls how a node with more than one incoming edge decides whether to fire — see the shared walker below. An **edge** (`{id, from, to, condition, value}`) decides whether a run continues down that branch: `condition` is `'success'` (exit 0) | `'failure'` (non-zero exit) | `'always'` | `'exitCode'` (exit code equals `value`, a number) | `'outputContains'` (stdout+stderr includes `value`, a string) — the sanitizer forces `value` to `null` for the three conditions that don't use it. `pipeline.schedule` (same `ScheduleConfig` shape a snippet's own schedule uses) and `maxConcurrency` (0 = unlimited) are pipeline-level, not per-node — see Execution below.

**Renders as a full-window "screen"** (`PipelinesModal.tsx`, `.screen` in `style.css`), replacing the snippet list for as long as it's open rather than a small centered `.modal` dialog or a separate OS window — a graph editor needs real room, and both a fixed-size modal and a genuinely separate `BrowserWindow` were tried and moved away from before landing here, for consistency with every other full-screen feature in this app. Unlike the Group editor (a `.modal` — see the Groups bullet above), the Pipelines editor stays part of this same screen: **Editor** (`PipelinesModal.tsx`'s `EditorView`, working copy in local `useState` — `nodes`/`edges`/`selection` — written back to `usePipelinesStore.ts`'s persisted list only on Save, so Cancel/Back is a true discard) shares one list-view/editor-view split with the plain pipelines list (`.screen-header`/`.screen-body`/`.screen-footer` for the list; the editor swaps `.screen-body` for `.pipeline-toolbar`/`.pipeline-editor-body` since the canvas+inspector need the full remaining height, not a scrolling body) — a graph editor's own working copy is heavier than a group's plain name/description/member-list form, and staying a screen is also why opening the Group editor FROM a pipeline node's own Inspector ("Edit group…") never has to touch this screen at all: the `.modal` just layers on top, leaving the in-progress graph untouched underneath.

Running a **saved** pipeline from the list still closes this screen first (`closePipelinesForRun()`, `usePipelinesStore.ts`), the same way `GroupsModal.tsx`'s `runGroup()` closes Groups before handing off to `useBatchStore.ts`'s results modal — there's no canvas open in the list view to watch a run paint onto anyway. Running from **inside the editor**, though, leaves the screen open (`runWithGate(pipeline, false)` — see `usePipelineParamGate()`'s own header comment): `.modal-overlay` renders above `.screen` (see `style.css`), so the results modal still layers correctly on top, and leaving the canvas mounted is what lets `pipelineEngine.ts`'s live run-status painting (below) actually have something to paint onto instead of running against an already-unmounted canvas.

**The canvas itself (`pipeline/PipelineCanvas.tsx`) is built on [`@xyflow/react`](https://reactflow.dev) (React Flow)** — replaced an earlier hand-rolled implementation (absolutely-positioned `<div>`s dragged via raw `mousedown`/`mousemove`/`mouseup`, an overlaid SVG recalculated via `getBoundingClientRect()`, a hand-rolled Kahn's-algorithm auto-arrange) that this file used to document as the app's "one deliberately-not-fully-declarative piece of UI." React Flow now owns dragging, connecting, pan/zoom, multi-select, keyboard delete, and the minimap; [`dagre`](https://github.com/dagrejs/dagre) (`lib/pipelineLayout.ts`) replaced the hand-rolled topological layout for **Auto-arrange**.

`PipelineCanvas.tsx` is **fully controlled**: `pipelineFlow.ts`'s `toFlowNode`/`toFlowEdge`/`fromFlowNode`/`fromFlowEdge` translate between this app's own persisted shape (`PipelineNode[]`/`PipelineEdge[]`, what `EditorView` keeps in `useState` and what `pipelineEngine.ts` runs) and React Flow's own `Node[]`/`Edge[]` shape every render — there's no second, parallel copy of the graph living in React Flow's internal store. `toFlowNode()` picks React Flow's own node `type` from `PipelineNode.kind` and builds a kind-specific `data` shape (`PipelineFlowNodeData`, a discriminated union) carrying only what that kind's own component needs to render — `fromFlowNode()` reconstructs the full `PipelineNode` by merging the flow node's `data` back onto the *existing* same-id `PipelineNode` (passed in by `PipelineCanvas.tsx`), which is what keeps a plain drag (position-only change, `data` untouched) from silently resetting `retries`/`joinMode`/etc. to their defaults — those fields are Inspector-edited directly on the working-copy array, never routed through `data` at all. `nodeTypes` in `PipelineCanvas.tsx` maps all five kinds to their own component: `PipelineStepNode.tsx` (icon, tag color, shell label, same look `Card.tsx` uses for a snippet) and the four plainer `PipelineDelayNode.tsx`/`PipelineGateNode.tsx`/`PipelineSubPipelineNode.tsx`/`PipelineGroupNode.tsx` (`.pipeline-mini-node` in `style.css` — shorter than `.pipeline-step-node`, no avatar/tag-color since there's no snippet to color by) — every one of them still carries the two `Handle`s (`id="in"`/`id="out"`) `pipelineFlow.ts`'s edges expect. **Gotcha**: `pipelineFlow.ts`'s React Flow `type` for a `group`-kind node is the string `'groupRun'`, not `'group'` — `@xyflow/react` reserves the literal `'group'` type name for its own built-in parent/container-node feature and ships default CSS for it (a visible border), which silently applied itself on top of `.pipeline-mini-node`'s own styling the one time this was tried with the obvious name. `PipelineNode.kind` itself stays `'group'` (that's this app's own persisted vocabulary, unrelated) — only the React Flow-facing type string had to change. `PipelineConditionEdge.tsx` (custom edge type `'condition'`) draws a bezier path plus a floating pill label (`EdgeLabelRenderer`) colored by condition, clicking it selects the edge and right-clicking removes it.

**Three real gotchas from this rewrite, all fixed and worth knowing before touching this code again:**
- **Every node needs both `width`/`height` *and* `measured: {width, height}` declared up front** (`pipelineFlow.ts`'s `toFlowNode()`), matching `.pipeline-step-node`'s own CSS size exactly. `width`/`height` alone lets React Flow skip its own ResizeObserver-based auto-measurement pass, but React Flow diffs a fresh DOM measurement against `node.measured` (not `node.width`/`height`) to decide whether a node's size "changed" — leaving `measured` unset made the very first real measurement look like a change, which fired an `onNodesChange` `'dimensions'` event that round-tripped back through this app's own `setNodes` as a brand-new `PipelineNode[]` array, rebuilding every node's identity again before its connection-handle bounds could ever stay measured. The visible symptom was an edge that existed correctly in state (selectable, shown in the Inspector) but never painted a `<path>` — `getEdgePosition` silently came back null because it needs a resolved handle position, and the handle bounds needing measurement kept getting reset before they were used.
- **`toFlowNode`/`toFlowEdge` must not hand back a brand-new object for a node/edge nothing happened to.** `PipelineCanvas.tsx` caches each converted node per id (`nodeCacheRef`) and reuses the previous object whenever neither the source `PipelineNode` nor its `selected` flag changed; `handleNodesChange`/`handleEdgesChange` mirror this on the way back, reusing the *original* untouched `PipelineNode`/`PipelineEdge` for any item `applyNodeChanges`/`applyEdgeChanges` didn't actually touch instead of rebuilding every item via a plain `.map()`. Skipping this compounds the `measured` issue above (handleBounds resets for the *whole graph* on any single change) and, during an actual connection-drag gesture (which fires `onNodesChange` repeatedly per animation frame), spiraled into "Maximum update depth exceeded" (React error #185) — confirmed via this rewrite's own testing.
- `fitViewOptions`/`proOptions`/`deleteKeyCode` passed to `<ReactFlow>` are hoisted to module-level constants, and `onSelectionChange`/`onNodeContextMenu` are `useCallback`'d — React Flow's internal selection-listener effect is keyed on these exact prop references, so a fresh inline object/array/function on every render re-triggers that effect every render, which formed the other half of the same #185 loop while this rewrite was first built.

**Auto-arrange** (toolbar) lays every node out left-to-right via `dagre` (`lib/pipelineLayout.ts`), then re-fits the viewport (`fitViewSignal`, bumped on Auto-arrange and on adding a node — the `<ReactFlow fitView>` prop only ever fires its one-shot initial fit, which for a brand-new pipeline happens against zero nodes).

Clicking a node or an edge's label (as opposed to dragging/right-clicking) selects it, which drives the **inspector side panel** — the actual discoverable way to rename which snippet a step points to ("Change step…", reopens the same snippet-picker "+ Add step" uses), duplicate a step, connect it to another step ("Connect to…" — see below), delete a step/connection, or change a connection's condition type and its `value` via a real `ThemedSelect` + input rather than a tiny canvas label you'd have to already know to click. Right-click-to-delete on a node still works too, as a fast path once you know it's there. "+ Add step"/"Change step…"/"Connect to…" all go through one shared, generalized picker menu (`SnippetPickerMenu` in `PipelinesModal.tsx`) built on the same `.context-menu`/`.context-menu-item` look `CardContextMenu.tsx`'s Radix-based context menu uses (a separate, self-contained component — no shared code with it beyond the CSS classes).

**Connecting two steps has two paths, not one**: dragging from a step's "out" handle onto another step's "in" handle on the canvas (React Flow's native `onConnect`/`isValidConnection`, validated the same way as the Inspector path below), *and* the Inspector's explicit **"Connect to…"** button, which opens the same picker menu as "+ Add step" (items = every *other* step in this pipeline) and wires the connection via a click alone. Both funnel through `lib/utils.ts`'s `tryCreatePipelineEdge()` (self-connection, duplicate-connection, and cycle checks — `pipelineEdgeCreatesCycle()`, pure, unchanged from the original implementation), so neither path can create an invalid edge the other one would have rejected. The button exists as a reliable, precision-drag-free alternative for this pipeline's set of steps — keep it even if you're tempted to remove it as "redundant" once dragging feels solid, since a small/compact canvas (this app's `.modal-pipeline` isn't huge) makes a tiny handle-to-handle drag genuinely fiddly at some zoom levels.

**Execution is split into a shared pure core plus two thin per-environment adapters** — the same "what runs next" decision needs to happen identically whether a human is watching (the editor's own "Run" button) or nobody is (a scheduled pipeline), and the only thing that actually differs between those two is *how* one node gets executed, not the graph logic itself:

- **`@shared/pipelineWalk.ts`'s `walkPipeline(nodes, edges, {runNode, maxConcurrency, onNodeStart, onNodeDone, onEdgeWalked})`** is the pure graph walker — no DOM, no IPC, no Node API, just `PipelineNode`/`PipelineEdge`/`RunResult` (already-shared types) plus an injected `runNode` callback that actually executes one node and reports a `NodeOutcome` (`{kind:'ran', result}` or `{kind:'skipped'}` — a skip is a dead end, no outgoing edge is evaluated from it, same as a skipped batch-run row today). Starts every root node (no incoming edge) in parallel; after each node finishes, follows any outgoing edge whose condition matches (`edgeSatisfied()`, also here) to reach the next node(s). `joinMode: 'any'` (default) fires a node the first time any satisfied incoming edge reaches it — this pipeline's original OR-semantics. `joinMode: 'all'` is a real AND-join: the node only fires once *every* incoming edge's source has finished *and* every one of those edges is satisfied; if any required predecessor's edge doesn't satisfy, that join never fires, permanently (tracked via the same `started` guard that prevents double-execution — see the file's own comments for why the two synchronous-until-the-first-`await` code paths can't race each other for this). `maxConcurrency` (0/undefined = unlimited) throttles actual node execution (not graph traversal) via a small counting semaphore around the `runNode` call. Also exports `withRetries()`/`sleep()`, small enough to be genuinely shared by both environments' own step-node handling.
- **`lib/pipelineEngine.ts`'s `runPipelineGraph(nodes, edges, opts)`** is the renderer's interactive adapter: its `runNode` switches on `node.kind` — `step` adds a live row (`useBatchStore.ts`'s `addRow`/`setRowRunning`/`setRowDone`, wrapped in `withRetries` when `node.retries > 0`) and skips a still-parameterized snippet unless `opts.values` (see pipeline-level params below) already has every name it needs; `delay` adds a label-only row (`addLabelRow`) and `sleep()`s; `gate` adds a row via `addGateRow()` and `await waitForGate(rowId)` — a real Promise that only resolves when the user clicks Continue/Abort in `BatchModal.tsx`'s `ResultRow` (`resolveGate()`); `pipeline` fetches the referenced pipeline fresh (`getPipelines()`) and recursively calls `runPipelineGraph()` again on it (own `visitedPipelineIds` threaded through for a runtime cycle guard, defense in depth alongside the save-time check below), rendering as one `▸ <name>` label row whose result reflects the whole sub-run. Live progress is painted straight onto the already-rendered React Flow DOM via `onNodeStart`/`onNodeDone`/`onEdgeWalked` callbacks that toggle `pf-run-active`/`pf-run-ok`/`pf-run-error`/`pf-run-skipped`/`pf-edge-walked-*` classes on `.react-flow__node[data-id]`/`.react-flow__edge[data-id]` — the same "imperative DOM escape hatch for fast-changing live status" this app already uses for background-process output (`processEngine.ts`) and a card's own run output (`runEngine.ts`), chosen over threading a `runStatus` prop through `pipelineFlow.ts`/every node component for exactly the same reason those two didn't lift their own hot paths into component state either.
- **`main/pipelineRunner.ts`'s `runScheduledPipeline(pipeline)`** is the main-process unattended adapter (used by `scheduler.ts`'s tick, below) — no UI, no gate wait (a `gate` node is always skipped unattended), `step` nodes run via `unattendedRun.ts`'s `executeSnippetOnce()` + `applyCaptures()` directly (no IPC round-trip needed, it's already in the main process), `pipeline` nodes recurse via `readPipelines()`. Logs **one** aggregate history entry for the whole run (`"<name> (scheduled pipeline)"`), not one per step, matching how a multi-step snippet's own sequence already logs at the sequence level, not the step level.

A parameterized `step` node with no matching `opts.values` is skipped, same "no prompting in an unattended context" rule documented above for scheduler/batch/group runs — but see **pipeline-level parameters** just below for how an *interactive* run avoids hitting this in the first place.

**Pipeline-level parameters**: `lib/utils.ts`'s `collectPipelinePlaceholders(nodes, snippets)` scans every `step` node's own snippet for `{{placeholder}}`s once, up front (deliberately not recursing into a `pipeline` node's own sub-pipeline, or a `group` node's own members, for the same reason — see that function's own comment on why). `PipelinesModal.tsx`'s `usePipelineParamGate()` hook (shared by both `ListView`'s "Run" and `EditorView`'s "Run") checks this list before ever calling `runPipelineGraph()`: zero names → runs immediately; otherwise it gates the run behind a small modal reusing `Card.tsx`'s own `ParamForm.tsx`, collects the values once, and passes them all the way through (including into a recursive `pipeline`-node sub-run, or a `group` node's own members) as `opts.values` for `pipelineEngine.ts`'s `step` (and `group`) cases to substitute via `substituteAll()` instead of skipping the node outright — a group member with no matching value is skipped individually, same as a bare `step` node would be.

**Cross-pipeline cycle safety** has two layers: `lib/utils.ts`'s `pipelineReferenceCreatesCycle(pipelines, editingId, subPipelineIds)` runs at save time (`PipelinesModal.tsx`'s `save()`) — same "does X eventually reach itself" DFS shape as `pipelineEdgeCreatesCycle()` above, just one level up (across pipelines' own `pipeline`-kind nodes instead of within one graph) — and is also what the Inspector's "Change target…"/toolbar's "+ Sub-pipeline" pickers pre-filter their own candidate list with, so you can't even select a cycle-creating target in the first place. The runtime `visitedPipelineIds` guard mentioned above is defense in depth for a pipeline edited into a bad state some other way (e.g. hand-edited JSON).

Running from the Pipelines **list** hides the Pipelines screen first (see above); `lib/events.ts`'s generic `onBatchModalClosed`/`emitBatchModalClosed` plus `usePipelinesStore.ts`'s `closePipelinesForRun()`/`consumePendingReopen()` is what reopens it, back to the list, once the results modal is dismissed — `pendingReopen` is what lets that subscription (which fires for every batch/group/tag-run results-modal close, not just a pipeline's own) tell "reopen" apart from "leave it closed, the user just backed out with the header's own Back button." Running from the **editor** doesn't hide anything in the first place (see above), so there's nothing to reopen there.

**Scheduling a whole pipeline**: `Pipeline.schedule` reuses the exact `ScheduleConfig` shape/UI a snippet's own schedule does (a collapsible "Settings" panel in `EditorView`'s toolbar, alongside `maxConcurrency`) — `scheduler.ts`'s `tickScheduler()` checks due pipelines the same way it checks due snippets (`isScheduleDue()`, unchanged), calling `main/pipelineRunner.ts`'s `runScheduledPipeline()` instead of `unattendedRun.ts`'s `runUnattended()` for the ones that fire.

### The scheduler (`src/main/scheduler.ts`)

`startScheduler()` sets up `setInterval(tickScheduler, SCHEDULE_CHECK_INTERVAL_MS)` (30s), called once from `src/main/index.ts`. Each tick, `tickScheduler()` re-reads `readSnippets()` fresh from disk (so it always sees whatever the renderer most recently saved via the normal `save-snippets` IPC — there's no separate scheduling IPC/config channel), skips anything not `schedule.enabled`, and for the rest checks `isScheduleDue(schedule, now)`:
- `interval`: due when `now - lastRunAt >= intervalMinutes * 60000` (or never run yet).
- `daily`: due once `now` has passed today's `dailyTime` slot and `lastRunAt` isn't already past that slot (so it fires once per day, not on every tick after the time passes).
- `cron`: `cronMatches(expr, date)` delegates to [`croner`](https://github.com/hexagon/croner)'s `Cron` class (`new Cron(expr, { paused: true }).match(date)` — `paused: true` so the point-in-time check never actually schedules anything; a malformed expression throws in the constructor, caught here so `cronMatches` keeps its own "never throws, just returns false" contract) plus a 55-second debounce against `lastRunAt` so the same minute can't double-fire. This replaced a hand-rolled 5-field-only matcher — `croner` additionally understands named months/days (`MON`, `JAN`, …) and 6/7-field patterns, not that this app's schedule UI exposes those yet.

A due snippet runs via `unattendedRun.ts`'s `runUnattended(snippet, 'scheduled')` (see below) — no confirmation of any kind needed, see above — after which `tickScheduler()` itself bumps that snippet's `schedule.lastRunAt`/`lastRunAt`/`runCount` and does the tick's one `writeSnippets()` at the end, covering every snippet that ran this tick in a single write.

### Unattended runs & external triggers (`src/main/unattendedRun.ts`, `src/main/triggerServer.ts`, `src/main/fileWatcher.ts`)

`runUnattended(snippet, label, notify?, paramValues?)` is the shared "run this snippet with no one watching" path — factored out of what used to be the scheduler's own `runScheduledSnippet()` once `triggerServer.ts` (below) needed the exact same behavior: run it (steps loop or single call), append it to history as `"<name> (<label>)"` (e.g. `"(scheduled)"`/`"(triggered)"`), and fire an Electron `Notification` (unconditionally — independent of the renderer's `notificationsEnabled` preference, which the main process can't see anyway) whose click reopens the launcher on run history. **It deliberately does not touch the snippet's own `lastRunAt`/`runCount`** — an earlier version of this refactor had it do its own independent `readSnippets()`/`writeSnippets()` for that, which raced whichever caller (the scheduler) did its own end-of-tick write and silently lost one side's update. Bumping those two fields is left to each caller, on the same in-memory snippet object it's about to write back anyway — see `tickScheduler()` above and `triggerServer.ts`'s handler for the two current examples.

**A `{{placeholder}}` in an unattended snippet is resolved, not just checked for.** `resolveSnippetForUnattended(snippet, variables, overrides?)`/`substituteWithVariables()` (also in `unattendedRun.ts`) substitute every name against, in order: (1) `overrides` — the calling context's own fixed values (a schedule's or file-watch trigger's `paramValues`, see `@shared/types/paramValues.ts`, set once when configuring THAT schedule/trigger and never shared outside it), then (2) a matching saved global variable (Settings → Manage variables). Only a name with *neither* is left unresolved, and only then does `runUnattended()` skip the run outright — logged as a history entry ("Skipped — missing value(s) for: …") plus an optional notification, rather than either running the shell with literal `{{name}}` text still in it (a guaranteed syntax error, what used to happen here) or blanket-skipping every parameterized snippet regardless of whether a value was actually available. Every unattended path shares this exact resolution order: the scheduler (below, snippet schedules) and `main/pipelineRunner.ts` (pipeline schedules — one pipeline's own `schedule.paramValues` applies to every step across the whole tree, nested sub-pipelines included) both pass their own `paramValues` through; `fileWatcher.ts` and `triggerServer.ts` do the same (see their own paragraphs below).

`triggerServer.ts` is the optional external-trigger HTTP server (Settings → Triggers, `AppSettings.trigger`): a plain `node:http` server — no framework, two routes — bound to `127.0.0.1` only (never `'0.0.0.0'`, so it's reachable only from this machine, never the network). `POST /run/:snippetId` requires a token, matched against `AppSettings.trigger.token` and accepted either as an `X-Trigger-Token` header or a `?token=` query param; every request re-reads `readAppSettings()` fresh, so regenerating the token (`regenerate-trigger-token` IPC) takes effect on the *next* request with no server restart needed. `{{placeholder}}` resolution follows the shared order above, with this route's own `overrides` built from the request itself — every non-`token` query param, then (for a `Content-Type: application/json` body) that body's own `values` key (or the whole body, if it's a plain object), layered on top — so a caller can set a value for THIS call specifically without touching a saved variable at all. A name still unresolved after all of that gets a 422 explaining exactly where a value could come from (query param, JSON body, or a global variable). `POST /run-group/:groupId` (`handleGroupRun()`) runs every member of a saved Group the same unattended way — its own JSON body values are nested one level deeper, keyed by snippet id (`sanitizeGroupValuesBody()`: `{"<snippetId>": {"<placeholder>": "<value>"}, ...}`), and shared query params still apply to every member snippet that has a matching placeholder name. One aggregate `Notification` and JSON response cover the whole group run (per-member `{ok, exitCode}` or `{skipped, missing}`); the response status is 200 only if every member ran cleanly, 207 if any one was skipped or failed — mirrors `main/groupRunner.ts`'s scheduled-group equivalent almost exactly, just triggered by an HTTP request instead of a due schedule. `startTriggerServer`/`stopTriggerServer`/`isTriggerServerRunning` are the only entry points `ipc.ts`'s `set-trigger-config` handler and `index.ts`'s startup (`initTriggerServer`, if left enabled from a previous session) and `before-quit` (cleanup) need.

`fileWatcher.ts` is the optional file-watch trigger set (Settings → File-watch triggers, `storage/watchTriggers.ts`): one `fs.watch(path, {recursive: true}, ...)` per enabled trigger, keyed by the trigger's own **id** (not its path — two triggers can watch the same path with different snippets/debounce). `recursive: true` is supported on Windows, so a directory watch covers its whole subtree without this app walking it itself. Each trigger has its own debounce timer (`debounceMs`, default 800) coalescing a burst of change events (a save-triggered rebuild often touches many files at once) into a single `runUnattended(snippet, 'file-watch', true, trigger.paramValues)` call — `paramValues` set once per trigger (Settings → Automation's `WatchTriggerRow`, one row per placeholder the target snippet actually uses) resolves ahead of a saved global variable, same shared order as everywhere else. `syncFileWatchers()` re-reads the saved list and reconciles live `fs.watch` instances against it — called both at startup (`initFileWatchers()`) and after every save (`save-watch-triggers` IPC), never just once. `stopAllFileWatchers()` is called from `before-quit` alongside `stopTriggerServer()`.

## Renderer architecture

The renderer was migrated from framework-free native ES modules to TypeScript + React (Zustand for state) via a **strangler-fig migration**: each old `src/renderer/modules/*.js` file was ported to a real TS/TSX implementation under `src/renderer/src/`, temporarily leaving a one-line re-export shim (`export {...} from '../src/lib/whatever.ts';`) at the old path so every not-yet-updated consumer kept working. That migration has since fully **cut over**: every shim was deleted and every consumer rewired to import the real module directly, and the last two "shared mutable object, kept as plain JS" files (`state.js`, `dom.js`) were themselves converted to real TypeScript. There are no `.js` files left anywhere under `src/` — the exceptions in the whole repo are the three `build/*.js` electron-builder hook scripts (`afterPack.js`, `beforePack.js`, `generate-icon.js` — electron-builder `require()`s hooks directly at build time, outside the Vite/tsc pipeline, so they can't be TypeScript without adding a loader to the release pipeline for a few small scripts; not worth the risk).

If you ever see a stray re-export shim reappear (e.g. mid-conversion of a new module you're adding), that's a temporary scaffold, not a pattern to leave in place — finish rewiring its consumers and delete it in the same change, the way this migration's own cutover did.

`modules/state.ts` (the one shared mutable object — `state.snippets`, `state.filtered`, `state.selectedIndex`, `state.runningProcesses`, …) and `modules/dom.ts` (every `document.getElementById()` reference) are still real, plain, non-React modules that a large fraction of the app reads/writes directly — that part of the design is unchanged, only the file extension is. Fields move *out* of `state.ts` as each owning feature is ported to React — see the trailing comments in the file itself for where appearance settings, pipeline working-copy state, and update status each ended up (`useUiStore.ts`, local `useState` in `PipelinesModal.tsx`, `useSettingsStore.ts` respectively). A component ported ahead of `state.ts` itself bridges the gap via `hooks/useLegacyBus.ts`, which re-renders on the old `events.ts` bus's `snippets-changed`/`groups-changed` firing rather than subscribing to a store — read that file's header comment before reaching for it; it's explicitly a bridge, not the pattern to copy for new code.

### Entry point and mounting

`index.html` loads two module scripts: `app.ts` (the original entry point — wires the header's search/sort/group controls, does the initial `loadSnippets()`, and side-effect-imports whatever hasn't been fully absorbed into a React-rendered surface) and `src/main.tsx` (creates the `#reactRoot` React root rendering `App.tsx` — the toast host plus every modal/drawer, each a full self-contained overlay with no static markup left in `index.html` — then calls `legacyMounts.tsx`'s `mountLegacyReplacements()`, which mounts `SnippetList`/`TagFilters`/`FavoritesBar`/`SortModeSelect` each as their own React root directly into their pre-existing static containers, `#snippetList`/`#tagFilters`/`#favoritesBar`/`#sortModeSelect`, rather than replacing those elements). Several of those containers (`#favoritesBar` in particular) need their own `hidden` attribute toggled from inside the mounted component via a `useEffect`, since a React root mounted *inside* a container doesn't control that container's own attributes — `FavoritesBar.tsx` is the reference example if you add another one.

### State management: Zustand stores + `@shared/types`

Each ported modal/feature gets its own Zustand store under `src/renderer/src/store/`: `useHistoryStore`, `useVariablesStore`, `useDetailsStore`, `useEditorStore`, `useGroupsStore`, `useGroupDetailsStore`, `useUiStore`, `useSettingsStore`, `useBatchStore`, `usePipelinesStore`, `useHealthStore`, `useScheduleStore`, `useTerminalHistoryStore`, `useTemplateStore`, `useCommandPaletteStore`, plus `useSnippetsVersion` (a tiny redraw-signal store shared by `SnippetList`/`TagFilters`/`FavoritesBar`/`VariablesModal`/`GroupsModal` so any of them can force the others to re-derive from `state.ts` without a prop-drilled callback). `useUiStore.ts` persists theme/accent/density/blur/UI-scale/sound/notifications/dev-mode through a **custom Zustand `persist` `PersistStorage` adapter**, not the default single-JSON-blob storage — it deliberately keeps writing the exact original individual `localStorage` keys (`snippetRunner.theme`, `.accent`, `.density`, `.blur`, `.scale`, `.sound`, `.notifications`, `.devMode`) so an existing install's saved preferences carry over untouched. The main-process-owned hotkey setting stays main-process-owned (IPC + `app-settings.json`), not moved into renderer `localStorage`.

Every store and component imports data shapes from `@shared/types` rather than redeclaring them — `Snippet`, `RunResult`, `ProcessStatusValue`, etc. are the same types main is typed against.

### Cards, run engine, and the other deliberate DOM escape hatch

`src/renderer/src/components/Card/` (`SnippetList.tsx`, `Card.tsx`, `ParamForm.tsx`) is the ported `cards.js` — `SnippetList.tsx` exports `refresh()`/`updateSelectionStyles()` matching the exact function-shaped contract the original module had, since several call sites outside the component tree (`keyboard.ts`, `batch.ts`, `app.ts`) still call them that way rather than through a hook. Manual-reorder drag-and-drop is built on `@dnd-kit/core`/`@dnd-kit/sortable` (`SnippetList.tsx` mounts one `DndContext`/`SortableContext` around the card list, each `Card` calls `useSortable()` itself) — replaced the original hand-rolled native-HTML5-DnD version (per-card `state.dragSrcId` coordination, manual `before = e.clientY - rect.top < rect.height / 2` split-point math). `BatchModal.tsx`'s order-list drag (`OrderRow`) is the same pattern, independently mounted. Picking up the drag handle with keyboard focus + arrow keys works now too (`KeyboardSensor` — the original native-DnD handle had no keyboard path at all), and `state.dragSrcId`/`state.batchDragSrcId` are gone from `modules/state.ts` entirely — dnd-kit's own `DndContext` tracks active-drag state, no cross-instance shared mutable flag needed.

`src/renderer/src/lib/runEngine.ts` (ported from `run-engine.js`) is the third deliberate imperative-DOM piece, alongside pipelines and process streaming above: `runSingleSnippet`/`runSequenceSnippet` render a run's status/output/assertion/diff-toggle directly into the real DOM elements (`cardEl`, `output`, `copyOutputBtn`, `runBtn`) that `Card.tsx` hands it via refs, rather than driving that output through component state. This keeps the "render a run-before/this/run-after chain as labeled sections inside one output panel" logic (`runChainMember`, `buildChainSection`, `checkExpectation`, `renderLineDiff`'s diff-toggle) exactly as it worked before — a card's own output panel was always an imperative-DOM console, not really "UI state" in the React sense, and there's no reactivity benefit to lifting it.

### Renderer module map (current)

| Path | Owns |
|---|---|
| `modules/state.ts`, `modules/dom.ts` | shared mutable state / DOM refs — plain TS objects every not-yet-fully-React piece of the app reads/writes directly (see above) |
| `src/lib/utils.ts`, `icons.ts` | pure helpers, icon-string lookups |
| `src/lib/toast.ts` | `showToast()` — thin wrapper around `sonner`'s imperative `toast()`; `<Toaster/>` is mounted once in `App.tsx` (`unstyled` + `classNames` reusing the app's own `.toast`/`.toast-error`/`.toast-action-btn` look) |
| `src/lib/events.ts` | the tiny `EventTarget`-based bus (`onSnippetsChanged`/`emitSnippetsChanged`, etc.) |
| `src/lib/screens.ts`, `screenAnimation.ts`, `screenReturn.ts` | closing every full-window `.screen` as a group before opening a different one, skipping a screen's own fade-in for a screen-to-screen jump, and reopening whichever screen sent you into the Pipelines editor — see the dedicated section below |
| `src/lib/appearance.ts` | theme/accent/density/blur/UI-scale/sound/notification application, reads `useUiStore` |
| `src/lib/snippetsStore.ts` | load/persist/sort/filter/pin/duplicate/delete — no rendering |
| `src/components/Card/*.tsx` | card list rendering, selection, drag-reorder, the inline param form |
| `src/lib/runEngine.ts` | running a resolved command/sequence into a card's output panel — assertions, diff toggle, run-before/after chain rendering |
| `src/lib/variables.ts` | placeholder→variable value prefill/sync |
| `src/store/useBatchStore.ts` | the shared "run a list of snippets with live per-row output" engine + the results-modal, and `BatchModal.tsx` |
| `src/components/Card/CardContextMenu.tsx`, `CopyAsDropdown.tsx` | the right-click context menu and the "Copy as" dropdown, built on `@radix-ui/react-context-menu`/`@radix-ui/react-dropdown-menu` (replaced the old hand-rolled `lib/menus.ts`) |
| `src/lib/keyboard.ts` | the one global keydown listener |
| `src/components/Modals/*.tsx` + matching `src/store/use*Store.ts` | Editor, History, Variables, Details, Groups, Settings, Batch, Pipelines, Health, Schedule, TerminalHistory, Template, CommandPalette — Settings/Pipelines/Health/Schedule/TerminalHistory all render as full-window `.screen`s (see Theming below), the rest as small centered `.modal`s; **Groups is split**: `GroupsModal.tsx` (the browsable list) is a `.screen`, but adding/editing one moved to its own `GroupEditorModal.tsx`, a `.modal` — see the Full-window screens section below for why that split exists and `storage/groups.ts`'s own bullet further up for what it bought |
| `src/store/useGroupDetailsStore.ts`, `src/components/Modals/GroupDetailsModal.tsx` | a group's own read-only info panel (ID, schedule, every member snippet's own ID) — see `storage/groups.ts`'s bullet above |
| `src/lib/quickRun.ts` | "run this snippet right now, no card to render into" — shared by `ScheduleModal.tsx`'s "Run now" and `CommandPaletteModal.tsx`'s Enter-to-run; redirects to the real editor instead of running blind if the snippet is parameterized |
| `src/components/Modals/CommandPaletteModal.tsx`, `store/useCommandPaletteStore.ts` | Ctrl+K: fuzzy-searches (Fuse.js, same tuned options `snippetsStore.ts`'s own search uses) every snippet (Enter runs it via `quickRun.ts`) plus a fixed list of "jump to this screen" actions |
| `src/components/Modals/TemplateModal.tsx`, `store/useTemplateStore.ts` | "Generate variants" — turns one parameterized snippet into several concrete ones, one per value typed in (`CardContextMenu.tsx`'s "Generate variants…", shown only when the snippet has a placeholder) |
| `src/components/Modals/HealthModal.tsx` | the Health screen — pure read-only scan of `state.snippets` (missing `cwd` via the `path-exists` IPC, dangling `runBefore`/`runAfterThis`, last-run-failed via `getHistory()`) — never mutates, only links to the editor/Details |
| `src/lib/scheduleOverview.ts`, `src/components/Modals/ScheduleModal.tsx` | the Schedule overview screen, tabbed by source (Snippets / Pipelines / Groups) — `computeNextRun()` answers "when does this schedule next fire" (delegates to `croner`'s `Cron.nextRun()` for cron; simple date math for interval/daily); `scheduledSnippetRows`/`scheduledPipelineRows`/`scheduledGroupRows` each sort soonest-first within their own tab. Pipelines aren't preloaded at app boot the way snippets/groups are, so this screen fetches them fresh (`getPipelines()`) every time it opens. A Group's own Edit button opens `GroupEditorModal.tsx` directly (a `.modal`, no screen-closing needed); a Pipeline's own Edit button still calls `closeAllScreens()` first, since the Pipelines editor is still a `.screen` — see the Full-window screens section below |
| `src/components/Modals/TerminalHistoryModal.tsx` | "Import from terminal history" — lists real shell history lines (`getShellHistory` IPC → `shell/history-import.ts`) and turns checked lines into a new snippet (single command or multi-step, in original typed order) |
| `src/components/shared/ThemedSelect.tsx`, `ThemedCombobox.tsx` | every `<select>` and every free-text-with-suggestions field in the app, respectively — see **Theming** below |
| `src/components/shared/SnippetPicker.tsx`, `InfoHint.tsx` | every "pick a snippet" spot's shared search+tag-chip UI (`SnippetPickerMenu`/`SnippetPickerField`/`SnippetMultiPickerList`) — see **Theming** below; the small hover-for-details glyph replacing a permanently-visible explanatory line |
| `src/lib/processEngine.ts` | Start/Stop/Restart + live streaming output for `background` snippets |
| `src/lib/pipelineEngine.ts` | walks a pipeline's graph and runs it, branching on each node's result |
| `src/components/Modals/pipeline/*.tsx`, `src/lib/pipelineFlow.ts`, `pipelineLayout.ts` | the React Flow canvas (`PipelineCanvas.tsx`, `PipelineStepNode.tsx`, `PipelineConditionEdge.tsx`), the app-shape↔React-Flow-shape converters, and the `dagre` auto-arrange layout — see **Visual pipelines** above |
| `src/components/TagFilters.tsx`, `FavoritesBar.tsx` | tag-filter chips and the pinned-favorites bar |
| `modules/batch.ts` | select-mode toggle + the batch bar — same direct `dom.X.addEventListener` wiring style `app.ts` itself uses for the other header buttons, not a component |
| `app.ts` | entry point: header control wiring, initial load, side-effect imports of whatever isn't fully React-owned yet |

- `{{name}}`-style placeholders in a command/step are detected via `PLACEHOLDER_RE` (`lib/utils.ts`) and trigger `ParamForm.tsx`'s inline per-card form before running; substitution happens client-side before the (now-concrete) command is sent over IPC.
- **Select mode / batch run, and "Run all" on a group**: `state.selectMode`/`state.selectedIds` (still on the plain `state.ts` object — UI-only, never persisted) drive `modules/batch.ts`'s header wiring (select-mode toggle, the batch bar). Both a manually-selected batch and a group's "Run all" funnel through `useBatchStore.ts`'s `runBatchList(list, mode)`. The only thing filtered out is a parameterized snippet (marked "Skipped — needs input"), not a "dangerous" one (see above).
- **Run-after chaining**: see `runEngine.ts`'s `runChainMember` above — `runBefore`/`runAfterThis` point forward from a snippet to the one it runs before/after, walked with a `visited` `Set` (real cycle guard) plus a `MAX_CHAIN_HOPS` backstop.
- View/appearance preferences that aren't snippet data (`sortMode`, `groupView`, theme/accent/density/blur/UI-scale/sound/notifications/devMode) live in `localStorage` (`useUiStore.ts` for the appearance ones, plain reads/writes on `state.ts` for `sortMode`/`groupView`), not in any JSON file the main process manages — they're per-device display preferences.

### Cross-file import style in the renderer

Sibling `.ts`/`.tsx` files import each other **without** a file extension (`import { x } from './snippetsStore'`), the standard bundler-resolution style and the dominant convention across this codebase — match it for new code. The two exceptions, both load-bearing, not stylistic: type-only imports from the shared barrel always go through the `@shared/types` alias, never a relative path; and a handful of files still import `state`/`dom` from `modules/` with a relative path several directories up (e.g. `'../../../modules/state'`) since those two live outside `src/renderer/src/` — same extensionless style, just a longer path.

### UI-scale rendering (`applyAppearance()` in `lib/appearance.ts` + `.app-shell`)

Do **not** use CSS `zoom` on `#appShell` for the UI-scale feature — that was an earlier implementation and it left a visible gap at scale < 100% that exposed the transparent window's own black base paint on Windows (the same class of compositing quirk as the `hasShadow`/`thickFrame` issue below), because `zoom` shrinks the element's rendered box without anything filling the freed space. The fix: lay the element out *larger* than the viewport by the inverse of the scale factor, then transform-scale it back down to exactly 100%:
```js
const s = uiScale / 100;
dom.appShell.style.width = `${100 / s}%`;
dom.appShell.style.height = `${100 / s}vh`;
dom.appShell.style.transform = `scale(${s})`;
```
`.app-shell` has `transform-origin: top left` for this to anchor correctly. Because the transform lives on `#appShell` specifically, and every modal/drawer/toast renders as a **sibling** of it (via the `#reactRoot`, a direct child of `<body>`, not a descendant of `#appShell`), they're unaffected by the scale and always render at native 1:1 size, which is intentional.

### Theming (`style.css`)

Dark is the `:root` default. `:root[data-theme="light"]` overrides the palette variables explicitly; a *matching* `@media (prefers-color-scheme: light)` block, scoped to `:root:not([data-theme="dark"]):not([data-theme="light"])`, provides "System" mode by following the OS preference only when the user hasn't explicitly forced either theme. Keep both blocks' variable values in sync if you touch the palette — there's no CSS mixin to share them. The accent color is the one user-customizable palette value; `--accent-hover`/`--accent-soft` are derived from it via `color-mix()` (Chromium-only, fine since this is Electron-only) rather than computed in JS, so setting `--accent` from the renderer is enough on its own.

**`EditorModal.tsx`** (the Add/Edit snippet modal, the biggest form in the app) is built on `react-hook-form` + `@hookform/resolvers/zod`, not a single `useState<FormState>` + a `set(key, value)` helper spreading the whole object on every keystroke. Its `FormSchema` (local to the file) validates the *form's own* working shape, not the persisted `SnippetSchema` from `@shared/types` — deliberately a different schema, since the form keeps numeric-looking fields as strings (`expectExitCode`, `intervalMinutes`) and wraps `steps` rows as `{value: string}` objects (`useFieldArray` requires array items to be objects, not bare strings) — mapped onto real `Snippet` fields in the submit handler, the same way the original hand-written `save()` mapped its own `FormState`. Only the two checks that were already hard-blocking before (name required; command-or-steps required, depending on the multi-step toggle) are real `superRefine` validations with an inline `.field-error` message — everything else this form does (env/expect/schedule construction, the run-after/run-before text-to-id resolution, the runBefore/runAfterThis cycle check) still runs in the submit handler exactly as before, since those depend on the *other* snippets in the library at save time, not something a static per-field schema can check. **Gotcha if you touch this**: a `superRefine` issue on a whole `useFieldArray`'d array (e.g. `path: ['steps']`, not `['steps', i, 'value']`) lands at `errors.steps.root.message`, not `errors.steps.message` — RHF reserves the plain array key for per-row errors.

**Every `<select>` in the app is a `ThemedSelect.tsx`** (`src/renderer/src/components/shared/`), built on `@radix-ui/react-select` — a real accessible listbox (keyboard nav, typeahead) instead of a native `<select>`, styled via `.select-trigger`/`.select-content`/`.select-item` in `style.css`. This replaced the old `.select-wrap` + absolutely-positioned `<svg class="select-chevron">` workaround (the native arrow doesn't respect custom padding and renders visibly off-center against our styling) everywhere it was still used, including the header's own sort-order picker — `SortModeSelect.tsx`, mounted into a static container (`#sortModeSelect` in `index.html`) by `legacyMounts.tsx`, same pattern as `SnippetList`/`TagFilters`/`FavoritesBar` since the header itself isn't a React component. If you add another `<select>`, reach for `ThemedSelect` — there's no native one left anywhere in this app to copy from.

Every free-text-with-suggestions field (currently just EditorModal's tag input) is a **`ThemedCombobox.tsx`** (`src/renderer/src/components/shared/`) — a real text input plus a plain `position: absolute` suggestion panel (`.combobox-content`, styled to match `.select-content`/`.select-item`), replacing the old native `<input list="..."> + <datalist>` pattern. A browser's own datalist popup is entirely unstyled by CSS — no way to theme it to match this app's dark UI at all — so it looked jarringly out of place against everything else, unlike a native `<select>`'s arrow which at least respects *some* styling. Not built on `@radix-ui/react-popover` (not installed, and Radix has no Combobox primitive as of writing) — positioning is the same "plain CSS + an outside-mousedown listener" shape as `SnippetPicker.tsx`'s `SnippetPickerMenu` (below), just styled differently. Unlike `ThemedSelect`, this field tolerates arbitrary typed text alongside its suggestions (a brand-new tag), so it's a genuinely different component, not a `ThemedSelect` variant. EditorModal's "Run after this one"/"Run before this one" fields used to be `ThemedCombobox`es too (typed text resolved to an id case-insensitively at save time) — moved onto `SnippetPickerField` instead, since they only ever need to point at an *existing* snippet, never free text; see the next paragraph.

**Every "pick a snippet" spot in the app shares one component set, `SnippetPicker.tsx`** (`src/renderer/src/components/shared/`) — a plain `<select>`/free-scroll checklist doesn't scale once a library has dozens of snippets, so this gives every one of them the same fast search + tag-chip filtering instead of each hand-rolling its own:
- **`SnippetPickerMenu`** (+ the `PickerItem`/`SnippetPickerState` types) — the generic *floating* menu, for a call site that already manages its own anchor/open state and item shape. `PipelinesModal.tsx`'s Inspector is the one call site that needs this directly rather than through `SnippetPickerField` below, since its own picker points at snippets, OTHER PIPELINES, or OTHER STEPS depending on which button was clicked ("+ Add step"/"Change step…"/"+ Sub-pipeline…"/"Connect to…") — not always snippets, so the menu itself stays generic and the caller builds whichever `PickerItem[]` fits.
- **`SnippetPickerField`** — a self-contained "current value + Change… button" field wrapping `SnippetPickerMenu`, for the common case of picking exactly one snippet: EditorModal's run-after/run-before fields (`clearable: true` — see below), Settings' file-watch trigger rows, Variables' computed-variable source. Owns its own anchor/open state, so a call site just needs `value`/`onChange`/`snippets`. `clearable` shows a small × button next to Change… whenever a value is picked — pass it only where "nothing picked" is itself a meaningful, valid state (an optional run-after/before target) rather than something the field always needs filled in (a trigger's own target snippet, which just isn't functional yet if empty) — those omit it and rely on their own "Remove"/delete-the-row affordance instead.
- **`SnippetMultiPickerList`** — the inline (not floating), checkbox-based sibling for picking *several* at once: `GroupsModal.tsx`'s member checklist. Same search box + tag chips as the menu, backed by the same `useSnippetFilter()` internal hook (so the filtering logic itself is written once, not copy-pasted between the floating and inline variants) — just renders always-visible checkboxes instead of a click-to-pick-and-close popup, since it's meant to sit directly in a form rather than appear on a click.
- **`snippetPickerItems(snippets)`** — the one converter nearly every call site above wants (icon + name label, tag, a `${name} ${tag} ${command}` search blob); a call site with a genuinely different item shape (other pipelines, other pipeline steps) just builds its own `PickerItem[]` instead.

**`[hidden]` vs. author CSS**: a rule as plain as `.field-row { display: flex; }` permanently defeats the `[hidden]` attribute's default `display: none`, because author stylesheets always win over the user-agent stylesheet regardless of selector specificity or source order. `style.css` has a blanket `[hidden] { display: none !important; }` near the top specifically to make `[hidden]` reliable everywhere in this app — if you ever see a `hidden`-toggled element stubbornly stay visible, check whether a class on it sets `display` and either drop that class in favor of `[hidden]`, or give the row its own non-conflicting class the way the schedule-type field-rows use `.schedule-field-row` instead of the generic `.field-row`. Don't reintroduce a `display`-setting class on an element that's also toggled via `hidden`.

### Full-window screens: closing them as a group, skipping the open animation, and returning to where you came from (`lib/screens.ts`, `screenAnimation.ts`, `screenReturn.ts`)

Three small, separate files solving three distinct bugs that all came from the same root cause: `.screen` (Groups/Pipelines/Health/Schedule/Settings/TerminalHistory — see the Theming section above) all share z-index 100, so which one paints on top when two happen to be open is decided by DOM/JSX order in `App.tsx`, not by which opened more recently — opening one while another is already open just renders it invisibly *behind* the other rather than replacing it. (A `.modal-overlay`, at z-index 150, never has this problem — it always renders above every `.screen`, which is why only screen-to-screen navigation needs any of this.)

- **`lib/screens.ts`'s `closeAllScreens()`** closes every screen unconditionally before something opens a different one on top of what might already be open — `DetailsModal.tsx`'s pipeline link (Details can be reached from Health, itself a screen, as well as directly from the main list) and `ScheduleModal.tsx`'s "Edit" button on a scheduled pipeline row both call this first. The Group editor stopped needing this the moment it became a `.modal` (see the Groups storage bullet above) — a modal never has the z-index conflict in the first place — but the Pipelines editor is still a full `.screen`, so anything that jumps into it from elsewhere still has to call this.
- **`screenAnimation.ts`'s `useScreenOpenAnimation(open)`** fixes a *visible* symptom of the same root cause: `.screen` fades in from `opacity: 0` over 150ms (a nice touch opening fresh from the main list), but in a screen-to-screen jump the OLD screen disappears instantly (no fade-out of its own) while the NEW one starts fully transparent — for a chunk of that 150ms neither is opaque and the main list flashes through underneath. `closeAllScreens()` and `screenReturn.ts`'s `reopenScreen()` both call `markScreenToScreenNav()` right before actually closing/reopening; every screen component calls `useScreenOpenAnimation(open)` and adds a `screen-no-anim` class (`animation: none`) to its outer div when it returns true. Implemented as a same-render ref-diff, not a `useState` lazy initializer — these screen components stay mounted for the life of the app (`App.tsx` renders all of them unconditionally; only their own `open` flag toggles what they return), so a plain "capture once at mount" pattern would only ever fire on the very first render, never on a *later* open.
- **`lib/screenReturn.ts`'s `ScreenReturnTarget`/`reopenScreen()`** is what lets Back/Save/Escape on the Pipelines editor jump straight back to whichever screen opened it (Details, Schedule, …) instead of always landing on the plain pipelines list — `usePipelinesStore.ts`'s `openPipelineEditorByIdFrom(id, returnTo)` stashes the target, `closePipelines()` calls `reopenScreen(returnTo)` on the way out. Replaced an earlier single-purpose `returnToDetailsId` field that only knew how to reopen Details, which broke the moment a second screen (Schedule) grew the same kind of link. **Only the Pipelines editor still needs this** — the Group editor's move to a `.modal` made the equivalent group-side tracking unnecessary entirely (closing a modal never disturbs whatever screen was already open underneath it; nothing to "return to" because nothing was ever hidden). If you're tempted to add a `ScreenReturnTarget` case for some *other* modal, stop and ask whether that modal even needs it — the answer is usually no.


### Windows-specific window setup (`src/main/window.ts`)

The launcher window is `frame: false` + `transparent: true` with CSS-drawn rounding/shadow (`.app-shell` in `style.css`), created `show: false` and only ever shown via `showWindow()`/`toggleWindow()` (hotkey, tray, or IPC) — don't flip `show: true` in committed code, that's a manual-testing-only knob. `hasShadow: false` and `thickFrame: false` are required on `BrowserWindow` — the default native shadow/thick-frame style paints a black rectangular artifact behind a transparent frameless window on Windows because it doesn't composite the alpha channel correctly. Don't re-enable them without re-verifying the corners. Neither disables edge-drag resizing, though — Chromium's own frameless-window resize hit-testing near the borders is independent of the native thick-frame style, so plain `resizable: true` is enough on top of `frame: false`.

**The window is user-resizable** (`resizable: true`, `minWidth`/`minHeight` = `MIN_WINDOW_WIDTH`/`MIN_WINDOW_HEIGHT`, currently 520×440 — see `window.ts`'s own comment for how that floor was picked: below it, the header's search-icon + 7 icon-buttons row alone needs ~325px, and `.search-input`'s CSS needs `min-width: 0` — the flexbox default `min-width: auto` otherwise refuses to shrink a text `<input>` past its browser-defined intrinsic size, which is what pushes sibling buttons off the header to be clipped by `.app-shell`'s `overflow: hidden` once the window gets small enough). The chosen size is persisted to `app-settings.json`'s `windowSize` (debounced 400ms on the `resize` event, flushed immediately on `close` as a backstop) and restored on the next launch, clamped to the current minimum in `createWindow()` in case it was saved by an older build with a smaller/no floor. `showWindow()`'s re-centering uses the window's *actual current* size (`getSize()`), not the original `WINDOW_WIDTH`/`WINDOW_HEIGHT` defaults, so a resized window keeps re-centering correctly on every show/hide cycle instead of snapping back toward where a 760×620 window would have centered. Content reflow needed no real work beyond the `.search-input` fix above — `.app-shell` and most of its children were already flexbox/`%`/`vh`-based (a side effect of the UI-scale feature needing relative sizing anyway), not hard-coded to the original fixed window size.

The tray/window icon is generated at runtime by a hand-rolled PNG encoder (`icon.ts`'s `buildIconPng` — raw RGBA scanlines + `zlib.deflateSync` + manual chunk/CRC32) rather than shipped as an asset file, so the app has zero external image dependencies. **The packaged `.exe`'s own icon is a separate thing** — electron-builder needs a real `.ico` file on disk at pack time (a `NativeImage` won't do), which `build/generate-icon.js` produces via the same circle-badge drawing logic (plus a touch of edge anti-aliasing, genuinely necessary once the same hard-edged math is blown up to a 256×256 icon) at 16/32/48/256px, packed into one `.ico` container. Wired in via electron-builder's `beforePack` hook (`build/beforePack.js`) regenerating `build/icon.ico` fresh before every pack, referenced by `build.icon` in `package.json`. `build/icon.ico` is gitignored (a build artifact, regenerated every run, not a checked-in binary) — but `build/*.js` is explicitly un-ignored (`.gitignore`'s `build/*` + `!build/*.js`) since `afterPack.js`/`beforePack.js`/`generate-icon.js` are real source. (This un-ignore is itself a fix — `build/` was entirely gitignored before, silently, so `afterPack.js` was never actually tracked despite this file documenting it as intentionally checked-in source; worth knowing if you ever wonder why a `build/` script isn't showing up in `git log`.)

The window hides (never closes) on blur/close/Escape unless `app.isQuitting` was set by the tray's Quit item — that flag is what distinguishes "hide" from "actually exit" throughout `src/main/`.

### Global hotkey (user-configurable) (`src/main/hotkey.ts`)

`registerHotkey(accelerator)` is the only place that calls `globalShortcut.register`/`unregister` — it unregisters whatever's currently active, tries the new one, and rolls back to the previous accelerator on failure (never leaves the app with *no* working hotkey without the caller finding out via its boolean return). At startup, `src/main/index.ts` tries the user's saved `appSettings.hotkey` first, then falls through a defaults array. The `set-hotkey` IPC handler (`ipc.ts`) is the only thing that persists a new accelerator to `app-settings.json` — a successful *startup* fallback does not overwrite the saved preference, so a combo that's temporarily taken by another app doesn't silently erase what the user actually asked for. (Registration can also fail entirely in some sandboxed/remote-desktop environments — the app still runs, just tray-only, and logs a note about it.)

### Self-update (`src/main/updater.ts`)

Wraps `electron-updater`'s `autoUpdater` with `autoDownload`/`autoInstallOnAppQuit` both **off** — check, download, and install/relaunch are three separate, user-triggered actions (Settings → Updates), never something that happens on its own in the background. Same "nothing runs unless you click it" stance as the rest of this app, just applied to the app updating itself instead of a command running.

Update feed is **GitHub Releases on this repo**, configured via `build.publish` in `package.json` (`provider: "github"`, `owner`/`repo` pointing at `whosm1ke/ElectronBasics`) — `electron-builder` writes `latest.yml` (version + installer hash) into the release alongside the NSIS installer whenever a release is published (see the `npm run release` note under **Commands**), and `electron-updater` reads that file to decide whether a newer version exists. Since the repo is public, an installed copy needs no auth to check/download.

`checkForUpdates()` guards on `app.isPackaged` first — running via `npm start` has no `app-update.yml` (that file only exists inside a packaged app's resources, generated from the `publish` config), so calling into `electron-updater` from source throws a confusing low-level error instead of the friendly "updates only work in the installed app" message this checks for up front.

Every `autoUpdater` event (`checking-for-update`, `update-available`, `update-not-available`, `download-progress`, `update-downloaded`, `error`) is forwarded to the renderer over one `update-status` IPC channel rather than being the return value of the `check-for-updates`/`download-update` IPC calls — those calls just kick off the corresponding `electron-updater` call and resolve almost immediately; the actual result arrives later via the event. `useSettingsStore.ts` keeps the last-received status so reopening Settings mid-download shows where things actually are instead of resetting to "Check for updates."

`quitAndInstall()` explicitly sets `app.isQuitting = true` before calling `autoUpdater.quitAndInstall()` — same defensive pattern as the tray's Quit item (`tray.ts`) — because `window.ts`'s `close` handler intercepts and just hides the window unless that flag is already set, which would otherwise silently swallow the quit the installer needs.

## Clean code in this project

This codebase was deliberately refactored from three large files (`main.js`/`preload.js`/`renderer.js`) into the current one-concern-per-file layout, then migrated to TypeScript + React on top of that same layout — the file-per-concern discipline predates and survived the framework migration. When adding or changing code here:

- **One concern per file, named for that concern.** If you're about to add a second unrelated responsibility to a file (e.g. bolting scheduling logic onto `shell/exec.ts`, or UI-state for a new modal into an existing store), stop and give it its own file instead. A module in this project should be describable in one sentence — most of them are, in the comment at the top of the file. Keep that comment current when you change what a file is responsible for.
- **Imports go one direction — no cycles**, in the renderer especially: Node's CommonJS/ESM cycles hand back a partial (possibly empty) export, which is a nasty class of bug either way, and Vite doesn't paper over it. `SnippetList.tsx`/`Card.tsx` render and are imported by many leaf modules for their `refresh()`/component exports — none of those may be imported back by them. When two modules genuinely need each other's behavior (the batch-run case: cards need to trigger a run, batch needs the card list's `refresh()`), pull the shared part into a third, lower-level module neither original one needs to import back — see `useBatchStore.ts`, which both `Card.tsx` and `pipelineEngine.ts` depend on without either needing to import the other.
- **Cross-cutting notifications go through the event bus, not a direct call chain**, for anything not yet on a Zustand store: `lib/events.ts` wraps a plain `EventTarget`; a module that mutates snippets calls `emitSnippetsChanged()` and doesn't need to know or care who's listening. Reach for this (or a store subscription, for fully-ported pieces) over a direct import when the relationship is "something changed" rather than "I need this specific value/function back."
- **Main-process storage modules all follow the same shape**: `ensure*File()` / `read*()` / `write*()`, a `sanitize*()` that backfills missing fields so old or hand-edited JSON never breaks the UI, applied on both read and write. The read side goes through `json-file.ts`'s `readJsonFileSafe()` (BOM-stripping, corrupt-file preservation) and the write side through its `writeJsonFileAtomic()` (write-file-atomic under the hood — temp file + rename, so a crash/power-loss mid-write can't leave the real file truncated) — every `storage/*.ts` module's `write*()` goes through this rather than a bare `fs.writeFileSync(path, JSON.stringify(...))`. Copy `storage/variables.ts` or `storage/app-settings.ts` as a template for a new persisted concern rather than inventing a new pattern — and add the matching shape to `src/shared/types/`.
- **Renderer DOM lookups outside React live in `modules/dom.ts`, never a scattered `document.getElementById` elsewhere.** If a not-yet-React piece of code needs a new element reference, add it once to `dom.ts` and import `dom` where needed. Inside a component, use refs the normal React way instead.
- **No template strings standing in for a templating engine, in the parts of the app that are still imperative DOM** (`runEngine.ts`, `processEngine.ts`, `pipelineEngine.ts`'s DOM-adjacent bits) — build nodes with `document.createElement`, direct property assignment, small `innerHTML` fragments for static icon markup only, matching the original style. Inside a component, write normal JSX — don't hand-roll DOM construction there. (`menus.ts` used to be a third example of this — it's gone; `CardContextMenu.tsx`/`CopyAsDropdown.tsx` are real componentized JSX now, per the rule's own last sentence.)
- **Comments explain *why*, not *what*.** The codebase leans on short header comments (what a file owns, and any non-obvious constraint) plus inline comments only where the "obvious" implementation would actually be wrong (e.g. why `terminal.ts` can't just `spawn()` directly, why `windowsVerbatimArguments: true` is required, why the pipeline canvas and process-output streaming stay imperative). Don't narrate straightforward code line-by-line; do explain a decision that isn't self-evident from reading it.
- **Prefer extending an existing small module/component over growing one past its stated concern.** If a file you're editing is growing past a couple hundred lines and picking up a second concern, that's the signal to split it (a new store, a new `lib/` file, a sub-component), not to keep appending.
- **Prefer the latest stable version of a dependency, and avoid deprecated APIs**, consistent with how this migration was done (TypeScript 7's native compiler, Vite 7, React 19, current `electron`/`electron-builder`/`electron-updater`). When a peer-dependency range forces an older minor (e.g. `@vitejs/plugin-react` pinned to the last 5.x release that supports Vite 7, since 6.x requires Vite 8), leave a comment on the pin explaining the constraint so a routine bump doesn't get silently reverted.

## Project structure

```
package.json              "main": "out/main/index.js"
electron.vite.config.ts    one config, three processes
tsconfig.json               solution file → tsconfig.node.json (main+preload) / tsconfig.web.json (renderer+shared)
.github/workflows/ci.yml    typecheck + test on push/PR
src/
  main/
    index.ts                entry point — pure wiring, no logic of its own
    window.ts                the launcher BrowserWindow: create/show/hide/toggle
    tray.ts                   tray icon + menu
    hotkey.ts                 global-shortcut registration with safe fallback
    icon.ts                    hand-rolled PNG encoder (tray/window icon, no assets)
    ipc.ts                     every ipcMain handler — delegates out, no logic
    scheduler.ts               the 30s background tick (interval/daily/cron) — snippets AND pipelines
    unattendedRun.ts            shared "run with no one watching" logic (scheduler + triggers + file-watch)
    pipelineRunner.ts             the unattended (scheduled) pipeline walker — @shared/pipelineWalk.ts's other adapter
    groupRunner.ts                the unattended (scheduled/triggered) group runner — every member run sequentially, one aggregate outcome
    triggerServer.ts             the optional loopback HTTP trigger server — /run/:snippetId AND /run-group/:groupId
    fileWatcher.ts                the optional fs.watch-based file-watch triggers
    computedVariables.ts          refreshes a "computed" variable by running its source snippet (manual + 30s interval)
    updater.ts                  electron-updater wrapper: manual check/download/install
    electron-augment.d.ts        Electron namespace augmentation (app.isQuitting)
    paths.ts, ps-quote.ts, env-utils.ts   small shared leaves
    shell/
      exec.ts                   the multi-shell execFile engine (incl. ssh)
      process-manager.ts         spawn-and-stream engine for background/long-running snippets
      terminal.ts                 opens a real, visible, interactive terminal window
      history-import.ts           reads real PowerShell/Git Bash history for the import screen
    storage/
      snippets.ts                 sanitizeSnippet() wrapper + DEFAULT_SNIPPETS + read/write
      history.ts, app-settings.ts, variables.ts, groups.ts, pipelines.ts, libraries.ts, watchTriggers.ts
    *.test.ts                    Vitest unit tests alongside the modules they cover
  preload/
    index.ts                  contextBridge — the only surface the renderer can reach, `satisfies ElectronAPI`
  shared/
    id.ts                       newId() — the one plain shared runtime value (not just a type) in shared/
    captures.ts                  extractCaptures() — pure regex-extraction for Snippet.captures, used by both processes
    pipelineWalk.ts               walkPipeline() — the pure graph-walking core both pipeline runners drive
    types/                      zod schemas (Snippet/Group/Variable/Pipeline/WatchTrigger + friends) and plain
                                 hand-written types, `@shared/types` barrel — imported by main AND renderer
  renderer/
    index.html, style.css
    app.ts                      entry point — header wiring, bootstrap, remaining side-effect imports
    modules/                     state.ts (shared mutable object), dom.ts (element refs), batch.ts (select-mode/batch-bar wiring) — see Renderer architecture
    src/
      main.tsx                   React entry — mounts #reactRoot, then legacyMounts
      App.tsx                    every full-overlay modal/drawer + the toast host
      legacyMounts.tsx            mounts SnippetList/TagFilters/FavoritesBar/SortModeSelect into their static containers
      components/                 Card/, Modals/ (incl. HealthModal, ScheduleModal, TerminalHistoryModal,
                                   TemplateModal, CommandPaletteModal, GroupEditorModal.tsx + GroupDetailsModal.tsx
                                   (Groups is split into a `.screen` list + two `.modal`s — see the Full-window
                                   screens section), pipeline/ (incl. PipelineGroupNode.tsx) — Settings is now one
                                   of these too, a `.screen` not a `.modal`), TagFilters.tsx, FavoritesBar.tsx,
                                   SortModeSelect.tsx, shared/ (incl. DetailsRow.tsx, SnippetPicker.tsx, ThemedSelect.tsx,
                                   ThemedCombobox.tsx, InfoHint.tsx)
      store/                       one Zustand store per ported feature
      lib/                         ported pure/imperative logic: runEngine, processEngine, pipelineEngine, pipelineFlow,
                                   pipelineLayout, scheduleOverview, quickRun, toast, keyboard, events, snippetsStore,
                                   appearance, icons, utils, variables, screens, screenAnimation, screenReturn
      hooks/useLegacyBus.ts        bridges the old event bus into React re-renders for early-ported components
out/     build output: out/main, out/preload, out/renderer (gitignored)
dist/    electron-builder packaging output (gitignored)
build/
  afterPack.js              electron-builder's afterPack hook — strips LICENSES.chromium.html
  beforePack.js             electron-builder's beforePack hook — regenerates icon.ico before every pack
  generate-icon.js           draws the app icon (same circle-badge as icon.ts) at 16/32/48/256px into icon.ico
  create-github-release.js   run directly (not an electron-builder hook) by the release scripts, between the version bump and the build — see the Commands section's own note on why this has to pre-create the release
  icon.ico                  gitignored build artifact — regenerated by beforePack.js, not checked in
  (all four .js files above are the repo's only intentional non-TS files — see Commands/toolchain notes: electron-builder require()s the hooks directly, outside the Vite/tsc pipeline, and create-github-release.js is a small standalone release-tooling script in the same spirit)
```

## Development

```bash
npm install
npm start
```

`npm run typecheck` and `npm test` before committing anything nontrivial — there's no pre-commit hook enforcing either. After editing, `npm start` hot-reloads the renderer; a main/preload change triggers electron-vite to rebuild and relaunch automatically.
